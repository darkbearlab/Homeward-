# 誓約系統（Oaths）— 設計文件

> 敗騎的**誓約**：刻在身與心上的約束。**綁定誓約**是一位騎士的本質（不可卸）；**自選誓約**是靠行動證明、解鎖後可帶的 build 層。
> 這份是獨立維護的目錄——條目會愈長，PLAN.md 只放摘要並指向這裡。
> **狀態：第一階段（誓約引擎 + 7 位騎士綁定誓約）已實作。** 配誓約 UI / 槽數升級 / 解鎖條件 / 自選誓約池仍待做。

---

## 1. 定位與機制

跟現有三個系統的關係：

| | 來源 | 生命週期 | 效果引擎 |
|---|---|---|---|
| **聖物**（已有） | run 中撿 | run 結束清 | Hooks |
| **meta 升級**（已有） | 榮譽買 | 永久、恆常 | `makePlayer` 直接加 |
| **誓約**（新） | **達成條件解鎖 → 出發前裝進槽** | 整個 run 生效、run 中鎖定 | **同一套 Hooks（重用聖物骨架）** |

誓約＝「可裝備的被動 build 層」：槽位有限、要取捨、靠成就解鎖——跟「一路買下去的 flat 升級」是**不同軸**，互補。

**使用者定案：**
- **命名**：整套叫**誓約**。
- **綁定誓約＝獨立常駐**：不吃自由槽，是「這位騎士的本質」。**這就是本質區分不同騎士的落點。**
- **選擇時機＝B**：選完出身後多一步「配誓約」畫面（把已解鎖的自選誓約裝進自由槽；綁定誓約自動補上），**run 中鎖定不可改**（＝「只有出發前能選」）。
- **槽位**：**綁定 1（常駐、不佔自由槽）＋ 自由基礎 2 → 用 meta 升級 `oathSlots` 一路升到 5**（+1/級 × 3 級）。
  - **複數格誓約（未來）**：部分強效誓約 `slotCost > 1`，佔多格＝擠掉別的，逼出取捨（base 2 時裝一個 2 格的就滿了）。裝備 UI 要能顯示/擋「剩餘格不夠」。

## 2. 資料模型（重用 Hooks）

```
OATHS = { id: {
  name, desc,
  kind: 'bound' | 'free',
  boundTo?: originId,          // bound 專用：綁哪位騎士
  slotCost?: number,           // 佔幾個自由槽（預設 1；未來有誓約佔複數格＝強效但擠掉別的）
  hooks?: { event: fn },       // 事件效果（寫法同 RELICS）
  mods?: (knight) => {},        // 開局改騎士數值（像 meta 升級的 kUp）
  unlock?: (rs) => bool,        // free 專用：run 結束查 rs（run 統計）是否達成
  unlockHint,                   // 解鎖條件的人話提示
  gate?: {...},                 // 「裝上後才啟動」的解鎖閘門（見 §8）
} }
```
- `applyOathLoadout()`：把「綁定 + 已裝上的自選」誓約的 hooks 掛上、mods 套到騎士。開局呼叫（仿 `applyRelicHooks`）。
- `meta.oathLoadout`（自選誓約 id 陣列，持久）、`meta.unlockedOaths`（已解鎖集合）。
- 綁定誓約由出身決定、不進 loadout 陣列、不可卸。

---

## 3. 綁定誓約（每位騎士的本質 · 各 1，常駐不可卸）

> 效果都盡量掛在既有旋鈕上（parryWindow / hyperArmor / backstabBonus / senseRadius / poiseDmg / reach…），好實作。數值全佔位、待玩測調。

### Base 4（現有出身）
| 騎士 | 綁定誓約 | 本質效果（草案） |
|---|---|---|
| **守備**（劍盾） | **壁壘之誓** | 招架窗口放寬、舉盾耐力消耗 −；「以盾為牆」——防守的每個選項都更寬容。 |
| **陷陣**（雙手斧） | **不退之誓** | hyperArmor 判定窗更長、體幹上限 +；硬換一刀後短暫傷害 +。 |
| **遊獵**（長弓） | **疾風之誓** | 移動中射擊精度不降、距離越遠傷害 +（風箏本命）。 |
| **習術**（火矢） | **咒能之誓** | 術冷卻 −、施法前搖 −；火矢穿透 +（唯一開局帶術的出身，本質就是施法）。 |

### 可解鎖 3（本批做出來的出身）
| 騎士 | 綁定誓約 | 本質效果（草案） |
|---|---|---|
| **長槍騎士** | **長驅之誓** | 在武器 reach 邊緣命中＝額外傷害 + 推開（逼退控距）；刺擊穿透 +。 |
| **弩騎士** | **重弩之誓** | 裝填加速、破甲 +；靜止蓄力的首發重擊。 |
| **術士騎士** | **法脈之誓** | 法杖魔法彈**穿透**（貫穿一直線）、射速 +；魔力不竭本就是它的定位。 |

### 未來 2（下一批要新增的變體 + 出身）
| 騎士 | 綁定誓約 | 本質效果（草案） |
|---|---|---|
| **遊俠 · 雙刃** | **暗影之誓** | 背刺/突襲傷害 ++、腳步近乎無聲；潛行擊殺後短暫加速（接殺）。**吃剛做的潛行/聽覺系統。** |
| **鎚騎士** | **破甲之誓** | 對披甲目標體幹傷害 ++、破甲、擊退 +；專治重裝的本命。 |

---

## 4. 自選誓約池（`kind:'free'` · 解鎖後可裝進自由槽 · 任何騎士通用）

> 靠 run 結束的條件解鎖（見 §5）。這裡是起始池，之後慢慢加。

| 誓約 | 效果（草案） | 解鎖條件（`unlockHint`） |
|---|---|---|
| **守護之誓** | 扈從崩潰門檻 −（更難潰逃）、護送/救援回報 +。 | 無人陣亡通關一次。 |
| **仁德之誓** | 義名(mercy)相關事件回報 +、救助更有效（回血/物資 +）。 | 以「高義名」結局通關（率眾光復 or 教堂會師）。 |
| **無畏之誓** | 低血時傷害 +（背水一戰，越險越猛）。 | 一個 run 內觸發瀕死保命（不屈）達 N 次，或低血通關。 |
| **復仇之誓** | 斬將（擊殺菁英）後短暫全能 +（傷害/移速/回耐）。 | 累計斬將 X 次。 |
| **堅忍之誓** | 跨關回血 +、耐力回復 +。 | 連續 N 關不進休息節點就通關。 |
| **掠奪之誓** | loot 精良度 +（`rollDropQuality` 權重上移）、榮譽 +。 | 一個 run 搜刮/取得裝備 X 次。 |
| **速決之誓** | 開場氣勢 +、開局移速 +（打閃電戰）。 | X 秒/幀內清掉一整關。 |
| **潛蹤之誓** | 腳步聲 −、被察覺半徑 −（在 meta 升級「潛蹤」之上再疊一層 build 向）。 | 一整關全程未被察覺就清掉（純潛行）。 |

（綁定與自選的效果會有重疊面向——刻意的：綁定給「這位騎士本來就擅長的」，自選讓你**往某個方向再壓**或**補短板**。）

---

## 5. 解鎖＝run 結束查條件（需要多記幾個 run 統計）

`metaOnRunEnd`（或收尾處）加一輪「查 OATHS 裡 `unlock(rs)` 達成、且未解鎖的 → 解鎖 + 提示」。`rs`＝一份 run 統計快照。

**現成可用**：`runStats.kia`（陣亡）、`runStats.events`、`runFlags.mercy`、`homecomingAxes().might`、結局 key（`pickEnding().key`）、`missionIndex`、`runFlags.ralliedKnight`、`runFlags.deathSaveUsed`。

**要新增追蹤**（開 run 時歸零、過程累計）：
- `kills` / `eliteKills`（斬將）
- `noRestStreak`（連續不休息關數）
- `stealthClears`（全程未被察覺清關次數）——接聽覺/警覺系統
- `lootTaken`（取得裝備/搜刮次數）
- `fastClears`（限時清關次數）
- `deathSaves`（本 run 觸發不屈次數，可由 `deathSaveUsed` 擴成計數）

## 6. 分階段實作（真正動工時）

1. ✅ **誓約引擎（已做）**：`OATHS` 表 + `boundOathFor` + `applyOathLoadout`（在 `makePlayer` 造好騎士後對 `i===KNIGHT_ID` 呼叫、每場重套）+ base 4 + 可解鎖 3 共 7 位騎士的**綁定誓約**。效果全走 `mods(k)` 改 per-unit 旋鈕（**絕不改 `u.weapon`＝WEAPONS 共用參照**）：
   - 新增旋鈕與套用點：`parryWinMul`→`parryWindowOf`、`reachMul`/`knockMul`→`meleeHitTest`、`pierceBonus`→近接與 `fireUnit` 子彈穿甲、`artCdMul`→術 CD、`castMul`→施法前搖、`magicPierce`→子彈 `pierceThru`＋`updateBullets` 貫穿（`b.hitSet` 防重複命中）。
   - 綁定誓約 id：`bulwark`(守備) / `unbowed`(陷陣) / `gale`(遊獵) / `incant`(習術) / `reachvow`(長槍) / `heavybolt`(弩) / `leyline`(術士)。
   - 出身選單卡片右上顯示「◈ 誓約 · 名稱」徽章（卡片夠高再多顯示本質敘述）。
   - 尚未做：自選誓約的 hooks 註冊（Phase 2 隨 loadout 一起，用穩定 id off-first 避免每場重複掛）。
2. **配誓約畫面（B）**：出身後一步，把已解鎖自選誓約裝進自由槽（綁定自動補、顯示鎖定）；run 中鎖定。`meta.oathLoadout` 持久。
3. **槽數升級**：`UPGRADES` 加 `oathSlots`（自由槽 base 2 → +1/級 → 上限 5）。
4. **解鎖條件框架**：run 統計補齊（§5）+ `metaOnRunEnd` 查解鎖 + 提示。
5. **內容擴充**：自選誓約池慢慢加；未來 2 位騎士（雙刃/鎚）+ 綁定誓約。

## 7. 待決/風險備忘
- 綁定誓約的效果 vs 出身起始裝的重疊：要確保「綁定誓約」有給到出身**沒有**的東西（行為/被動），不只是換句話說。
- 自選誓約若太強會蓋過綁定的身分感——數值上綁定應該最顯眼。
- 解鎖條件要「玩得到」：別設成幾乎不可能達成的門檻（沿用事件池的「下檔知情、不致命」精神）。

---

## 8. 延伸機制（未來 · 先記著，尚未設計細節）

### 8a. 複數格誓約
見 §1、§2 `slotCost`。強效誓約佔多格＝build 上的取捨。裝備 UI 要顯示每個誓約佔幾格、擋「剩餘格不夠」。

### 8b. 誓約作為「解鎖閘門」（gate）
§5 是**被動解鎖**（run 結束查條件 → 解到新誓約）。這裡是另一條路：**某些誓約「裝備上、且達成條件」才會觸發某個解鎖**——誓約本身是**鑰匙/前置**，把玩家導向特定玩法。範圍從小到大：
- **小＝成就**：裝上某誓約玩出特定打法 → 解成就/小獎（新自選誓約、榮譽、稱號…）。
- **大＝新篇章**：裝上某誓約 + 走完特定路線/結局 → **解鎖 C 面（反攻 / 擊退敵國）**——目前只有 A 面（敵境歸途）/ B 面（故土焦原）兩幕；C 面是「不只是回家，而是打回去」的第三幕，由誓約閘門驅動的巨型進度解鎖。
- 資料模型預留 `gate: { requireEquipped:true, cond:(rs)=>bool, unlocks:{kind,id} }`——判定放在 §5 同一輪 run-end 檢查，但多一個「該誓約有無裝備」前置。
- **注意**：C 面是跨系統的大工程（第三幕路線圖/敵人/地圖/敘事/結局），這裡只登記「誓約閘門是它的其中一個入口」，細節待專門規劃。
