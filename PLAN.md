# HOMEWARD — 分階段實作計畫

> 一句話定位：**戰敗騎士的歸鄉之路。你只操作騎士本人（近接為主、遠程為副），沿途收攏 AI 友軍；騎士陣亡即 run 結束。承襲 sleeper_roguelite 的單檔 Canvas2D 架構，攝影機完全沿用原版。**

本檔是開發脈絡與待辦。數值先求手感、集中於檔頂 `CONFIG` / `TUNE`。一步一步做，每階段有明確驗收。

---

## 技術基線（承襲原版，不更換引擎）

- **單一 `index.html`、原生 JS、Canvas 2D、零依賴零資產。** 已評估過 Phaser 等成熟引擎：引擎只能取代原版已完成的描繪/輸入層，無法提供真正的價值（戰術 waypoint AI、視界、撤離、meta 皆為引擎無關的自寫邏輯）。故沿用原版，不移植。
- **攝影機完全沿用原版**：旋轉跟隨 + 指標鎖定（FPS 式瞄準）。不退化為固定俯瞰。
- 保留原版資料驅動骨架：`CONFIG / WEAPONS / ABILITIES / EQUIPMENT / ITEMS / ARCHETYPES / PASSIVES / ENEMY_TYPES / FACTIONS / MAPS / ROADMAP` 與 `TUNE` 即時調整盤。

## 已確定的設計決策

1. **無附身**：全程只操控騎士本人。`controlledId` 固定為騎士。
2. **騎士死亡 = run 結束**（友軍死亡不直接結束 run）。
3. **近接為主、遠程副（弓/弩/魔法）可切換**；保留玩家的滑鼠瞄準技巧。
4. **友軍永久死亡（down = dead，同原版）**。
5. **輕量指令層**：跟隨 / 守住此處 / 集中攻擊目標 / 先行撤離。
6. **meta / 總部(HQ) / 結局敘事層** → 後置到 Phase 4，先完成「一段歸途」的可玩垂直切片。
7. **魔法（低度奇幻）** = 投射系「武器」（火矢等）+ `ABILITIES` CD 技（術/奇蹟）混用。

---

## 階段

### Phase 0 — 基線與去附身（土台）✅ 完成
- [x] 將原版 `index.html` / `editor.html` 置於本 repo 根目錄作為基線。
- [x] 移除附身/切換系統：`possess()` / `autoPossess()` / 數字鍵切換 / `CONFIG.switchHandoverMs` / `lastSwitchT` 全數移除；`controlledId` 只會是 `KNIGHT_ID(0)` 或 null。
- [x] 友軍恆為待機 AI 人格（`ARCHETYPE`）；騎士恆為唯一受控單位（`players[0]`）。
- [x] 勝負判定改主體：**騎士親自抵達撤離點 = 過關**；**騎士死亡 = run 結束**（`loseReason = '騎士陣亡'`）。
- [x] 遺物機制（原版機密檔案）以 `CONFIG.relicEnabled = false` 關閉，Phase 4 復活。
- [x] 確認保留無損：敵 AI、視界/迷霧、撤離、追兵軟計時、路線圖、休整/軍械庫/事件、陣營 FFA、攝影機。
- **驗收**：`node _check.js` 66/66 全綠（含全場景逐幀 update+draw、騎士抵達過關、騎士陣亡結束 run、扈從陣亡不結束、相機 `-π/2 − facing` 耦合未退化）。

**Phase 0 的設計決定（實作時定案，記錄備查）**
1. **騎士抵達撤離點時，尚在場的存活扈從隨之脫離**（不判定為損失）。理由：`idleTick` 的 `moveOnIdle:false` 代表扈從不會自己跟上，Phase 3 加「跟隨」指令之前若判損失＝每場全滅扈從；永久死亡只該由陣亡觸發。Phase 3 指令層完成後可再收緊。
2. **H = 騎士自走撤離**（自動導航＋滑鼠仍可轉視野，任一指令奪回）保留；**Shift+H 改為「命令扈從先行撤離」**（騎士不受影響）＝Phase 3「先行撤離」指令的雛形。
3. **E/Q 改為對全體扈從下姿態**（主動清場／防守）。原版是對「當前附身單位」下，去附身後騎士恆為手動、stance 對他無意義，否則變成死鍵。
4. **序章（第0關）的間諜敘事暫不動**（控制台「取得機密檔案」等），Phase 4 連同 HQ/結局一起重寫。

### Phase 1 — 近接戰鬥機制（手感核心）

> **開發方式（使用者定，2026-07-21）**：不在戰役裡做。先開一個**隔離的近戰試作場**（`?arena`，
> 目前是**開機預設**），關掉迷霧/時限/追兵/路線圖/序章/撤離判定，只留近接。手感調定後再把戰役接回來
> （屆時把檔尾 boot 的預設改回 `goMenu()`）。原本的主選單走 `?campaign`，或在試作場按 **M**。

- [x] **揮斬**：三段狀態機 WINDUP→ACTIVE→RECOVER，可 cleave 多目標。
- [x] **揮動軌跡**（使用者回饋後改）：不是「扇形整個推出去」，而是**刀刃在判定窗內真的移動**——
      `swingStyle:'sweep'` 由一側掃到另一側且**左右交替**（連揮＝→、←、→…）、`'thrust'` 刀尖沿正前方推出去。
      判定只吃「刀刃這一幀掃過的角度帶」（±`bladeHalfDeg` 刀刃厚度），不是整個扇形同時生效。
- [x] **盾格**：右鍵舉盾（持近接武器時取代望遠）；正面 `blockArcDeg` 內減傷 `blockResist`，移動/轉向變慢。
- [x] **招架（parry）**：舉盾後 `parryWindow` 內擋下＝完全格擋 + 攻擊者硬直。
- [x] **硬直**：`staggerT`（戰鎚命中 / 招架成功造成）——中斷揮擊、完全不能行動。
- [x] **擊退**：衰減速度而非瞬移＝仍吃 `collideWalls`，不會被推穿牆。
- [x] 節奏數值：前搖/判定窗/後搖/扇形角/觸及/cleave/擊退/硬直 → 全在 `WEAPONS` + `CONFIG.melee`，數值面板（`` ` ``）即時可調。
- [x] 近接武器差異化：劍(均衡・橫掃 100°) / 斧(慢・橫掃 160°・cleave 5) / 鎚(穿甲 0.95・大擊退・硬直) / **槍(突刺・reach 104・寬容僅 16°)** / 短劍(快・背刺 ×2)。
- [x] 盾＝`EQUIPMENT`（圓盾/鳶盾＝被動減傷 + 負重）。
- [x] **體幹（poise）** — 每單位一條會回復的體幹條，命中扣體幹；扣光＝**體幹破**（大硬直 + 受傷 ×1.6 處決窗 + 大幅擊退）。雜兵低（劍 3 擊）、重裝高。
- [x] **打斷** — 前搖中被命中＝這一刀直接被打斷；斧/鎚在判定窗中有 **hyperArmor**（免打斷、體幹傷害減半）＝重武器的身分。
- [x] **耐力（stamina）** — 揮擊/格擋/奔跑都要錢；歸零＝揮不動；**舉盾時耐力不足擋下 → 破防**。招架成功退還耐力。敵人不吃耐力（AI 不會管理，壓力改由體幹＋攻擊權承擔）。
- [x] **攻擊權** — 同時只有 `maxAttackers`(2) 隻近接敵能進攻，其餘在 `ringDist` 外繞行；揮完一刀必須退開 `tokenRest` 才能再拿＝進攻有節奏。
- [x] **攻擊預告 telegraph**（使用者定）— AI 出手前放出「會打到哪」的形狀，由中心**擴散填滿到末端**，
      **填滿的瞬間＝攻擊落下的瞬間**；只對玩家視線內的敵人顯示（背後偷襲仍然成立）。
      `telegraphOf(u)` 是通用接縫。**近接＝扇形、遠程＝長方形彈道走廊（會被牆截斷）**，兩者共用同一套填滿邏輯。
      為了讓遠程「有東西可預告」，AI 遠程加了瞄準蓄力期 `CONFIG.enemy.aimTime`（武器可覆寫 `aimTime`）——
      原版是看到就射、沒有前搖。**玩家手動射擊不受此限**（仍即時）；扈從與敵人對稱都要瞄。
      預告對象＝**騎士以外的所有單位**（扈從也畫：FF 開著，要看得到別站進自己人的火線）。
- [x] **友軍誤傷（全面開啟）** — 原版沒有，本作所有攻擊都會誤傷。關鍵區分：**索敵**仍走 `hostile()`
      （沒有人會「瞄準」自己人），但**傷害判定**吃到誰就是誰。斧的 cleave 會砍到自己的扈從＝設計上的張力。
- [x] **裝甲/穿甲（使用者定案 2026-07-21）** — 近戰**維持原版全機率制**（有效擋格率 = armor × (1−pierce)，
      繞背無視）：被彈開＝這刀白揮，「必須換鎚」的壓力最大。三個新決定：
      1. **裝甲彈開的那一擊，體幹傷害 ×`armorPoiseMul`(1.7)**——刀滑開但衝擊還在＝裝甲讓你
         「砍不動他，但更容易把他砸崩」。重裝的解法是鈍器，不是硬砍。
      2. **騎士有裝甲槽（`ARMORS` 資料表）**：布衣/厚襖/鎖子甲/胸甲/板甲，走與重裝敵人完全相同的
         擋格機制；代價＝移速、耐力回復、負重（奔跑）。穿越重越挨得住、但越快力竭＝安全 vs 續戰力。
      3. **盾格擋下的攻擊不再擲甲**（盾攔住的刀沒碰到甲，不做雙重防禦）。
      順帶修正：裝甲擲骰原本排在體幹**之後**（意外而非設計）；重排後彈開的擊退/體幹在同一處結算。
- [x] **穿甲軸的武器分工（近戰+遠程）** — 原版 rifle(0.5)/DMR(0.9) 的位置由中世紀武器接手：
      近戰＝鎚 0.95（近戰的 DMR）＞槍 0.55 ＞劍 0.35＞斧 0.30＞短劍 0.20；
      遠程新增 **長弓 0.15（反人員）/十字弓 0.85（破甲）**，沿用既有射擊管線（magazine 1 + reload＝搭箭/絞弦）。
      特定魔法（火矢等）Phase 2 一樣走此管線、給高 pierce 即可。
- [x] **Hooks 事件匯流排（roguelite build 的地基，使用者要求）** — 之後的聖物/祝福/詛咒全部走
      「訂閱事件 + 改 ctx」，引擎不再為每個效果開洞。已接線的事件（詳見 index.html 檔頂 Hooks 註解）：
      `attackStart / attackHit / hurt / blockHit / parry / armorDeflect / interrupt /
       poiseLow / poiseBroken / hpLow / stamEmpty / beforeDeath(可攔截＝復活通道) / death /
       kill(含 elite/boss 旗標與近戰/遠程歸屬) / missionStart / missionEnd`
      - 門檻類（hpLow/poiseLow）＝跌破觸發一次、回到門檻上重新武裝（`CONFIG.hooks`）。
      - handler 全包 try/catch＝一個壞掉的內容不拖垮戰鬥。
      - 「每場一次」狀態放 `Hooks.mission`（missionStart 自動清空）。
      - **RELICS 目錄 + 示範聖物「不屈」**（每場一次：致死傷害改留 1 HP）＝復活通道已打通；
        試作場 V 鍵切換。`knightRelics` run 中撿、`initRun` 清空。
      - `ENEMY_TYPES` 新增 `elite`（heavy 系）/`boss`（Phase 4）標記供 kill 事件歸類。
- [x] **試作場分場（使用者定）** — `ARENA_MODES` 登錄表：一個場地＝一條資料（地圖 + 配置 + 預設武器）。
      目前兩場：近戰（假人+劍兵）/ 射擊（上半標靶區 300–900 距離檔、下半對射區弓手×2 弩手×1）。
      **入口選單 scene `ARENASEL`＝開機第一個畫面**（自動列出所有 ARENA_MODES，含 desc/detail）；
      進場後 Esc 回選單、U 直接切下一個、`?arena=<key>` 直達。
      **之後的 gimmick 測試場＝加一條 ARENA_MODES + 手刻一張小圖**，選單與切換都自動生效、不改碼。
      新敵種 `archer`（長弓）/`arbalest`（十字弓）＝Phase 2 中世紀化直接沿用。
      踩雷：**假人與敵人同陣營，不能擺在對射走廊上**——會觸發 AI 的「射線被自己人擋住＝收火」，
      弓手永遠不放箭（實測抓到，故標靶區/對射區分上下半）。
- [ ] **彈道速度待調（使用者：之後再改，先把旋鈕做出來）**——旋鈕本來就在：
      `CONFIG.juice.bulletSpeedMul`（目前 **4**，原版為了讓槍戰接近 hitscan 而設）× 各武器 `projSpeed`。
      試作場遠程武器的參數表已列出「實際彈速」與「飛 500px 要多久」，調的時候有數字可依據。
      中世紀化後箭矢應該明顯比子彈慢（要看得見飛行、要能預判走位），估計倍率會降到 1~2。
- [ ] **待你試玩後調數值**——這是本階段真正的工作，程式只是把旋鈕接出來。
- [ ] 進階（後續）：突刺（拉近距離）、怯步、側步/迴避（先不做，若被圍毆時覺得沒有出口再加）。

**「戰鬥只是換血」的解法與實測（2026-07-21）**
根因＝**攻擊沒有成本、被打沒有代價**，所以雙方最佳策略都是「一直揮」。用兩條互相獨立的資源補：
耐力管「攻擊的成本」、體幹管「被打的代價」；再加攻擊權讓 1 對多 可讀。

實測（headless 模擬 1v3，騎士 vs 3 劍兵，各跑 40s）：

| 打法 | 結果 | 剩餘 HP | 招架 |
|---|---|---|---|
| 無腦一直揮 | 清場 | 52/100 | 0 |
| 會擋、會招架 | 清場 | **100/100** | 3 |

同樣清場，防守打法毫髮無傷＝**激勵梯度正確**。

**過程中抓到的失調（都是實測才看得出來的）**
- 擊退位移 ＝ `knock / knockDecay`，原本劍只有 **16.7px**，比兩個單位半徑和（24px）還短＝物理上不可能製造間合。已把 `knockDecay` 降到 6、各武器 knock 大幅提高。
- **短劍原本是 DPS 之王**（95，最高），與「快而弱＋背刺」的定位相反。已下修。
- 敵我共用同一份 `WEAPONS`，但這是「英雄 vs 群體」的遊戲：不下修的話騎士 **4 刀就被砍死**（100hp ÷ 30dmg），而清 3 隻雜兵要 9 刀＝**數學上必輸**。新增 `CONFIG.enemy.meleeDmgMul`(0.45)。
- `bladeHalfDeg` 9° 太薄、`swingTurnMul` 0.25 太死 → 對走動中的目標命中率極低（實測 39 揮只中 5）。已放寬到 14° / 0.4。
- `ringDist`(115) > 長劍觸及(78)＝等待中的敵人站在你打不到的地方。這是**刻意**的（要你自己踏進去），但代表玩家必須主動逼近，不能站著揮。

**友軍誤傷的必要配套：`CONFIG.friendlyFire.aiHoldFire`**
只打開 FF 而不管 AI，敵人會互相掃射把自己打光。AI 開火前必須檢查射線上有沒有自己人擋著。
實測（8 名遠程步兵擠成兩排對玩家開火 30s／扈從與敵人之間夾著騎士 30s）：

| | 敵人死於自己人 | 騎士被扈從誤殺 |
|---|---|---|
| `aiHoldFire` 關 | **6 / 8** | **是** |
| `aiHoldFire` 開 | **0 / 8** | 否（HP 100/100） |

- 只套**遠程**。近接不套——短距離掃擊如果也要求「扇形內不能有自己人」，AI 會完全不敢動手。
- **玩家的手動攻擊永遠不受此限**：自己的刀自己負責。
- **驗收**：`node _check.js` 94/94；手感由使用者在試作場實測認定。

**實作要點（供後續接手）**
- 揮斬掛在 **`fireUnit` 的最前面**（`if (W.melee) return startSwing(...)`），所以**玩家手動 / 扈從 overwatch / 敵人 AI 三條既有路徑全部自動共用**，沒有玩家專用碼。Phase 2 敵種中世紀化只要把 `ENEMY_TYPES.weaponId` 換成近接武器即可。
- `enemyTick` 的「衝完最後一段」改吃 `e.weapon.melee`（原本只看 `range ≤ meleeApproach`，長槍 reach 104 會被卡住）。
- `damageUnit` 加第 5 參 `opts {melee, attacker, knock, stagger}` 並**回傳是否被格擋**。
- 可讀性：前搖時刀停在起始側 + 淡淡的整段預告弧（**看得出要從哪邊掃過來**）；判定窗畫「已掃過的尾跡 + 前緣刀身」（**所見即所中**）；突刺畫一條推出去的刀 + 刀尖光點；舉盾畫護弧，招架窗內更亮。
- **踩雷：`rectWalls(x1,y1,x2,y2)` 吃的是對角座標，不是 `(x,y,w,h)`。** 第一版試作場誤當後者，`rectWalls(520,450,60,60)` 變成一個 460×390 的大方框，把騎士出生點整個封在裡面（實測：0/3 敵人與騎士之間無阻隔）。`_check.js` 已加兩道防線：柱邊長度 ≤80px、以及「至少一名敵人與騎士之間無牆」——注意**單看距離會空過**（敵人會貼到牆的另一側，距離看起來很近但永遠打不到）。

### Phase 2 — 中世紀化・四槽位・魔法

**已完成（A 批）**
- [x] **武器全面中世紀化**：槍械全數刪除。近接＝短刀(底線)/短劍/長劍/彎刀/戰鎚(1手)、雙手斧/長槍(2手)；
      遠程＝長弓/十字弓(2手)、**投槍(1手＝唯一能搭盾的投射武器)**；敵人專屬＝獠牙/木棍。
      終局解鎖 Siren（The Patriot 致敬）→ **喪鐘**（雙手大劍，`loud`＝每擊全場聽得見，放棄潛行換威力）。
- [x] **`hands` 欄位**（1/2）＋ **`OFFHANDS` 副手表**：小圓盾(窄護弧但招架窗最寬) / 圓盾(均衡) / 鳶盾(護弧最寬減傷最高但耗耐力、難招架)。
- [x] **四槽位**（使用者提案）：槽 1/2＝手部 set `{main, off}`，槽 3/4＝自由位（道具充能制 / 技能 CD 制）。
      換 set 要時間（沿用 `CONFIG.weaponSwap`），**各 set 自帶彈藥/裝填狀態**（換手不會弄丟裝填進度）。
      相容做法：`u.weapon*` 仍是「當前主手」的鏡像，既有戰鬥碼一行沒改。
- [x] **格擋必須有盾**（補掉「雙手斧與劍盾防禦力完全一樣」的漏洞）；
      **無盾＝武器招架**（使用者定案）：點按右鍵、窗僅 `wpWindow`（比盾窄）、成功回報與盾招架同等，
      **但空招會自己硬直 `wpFailStagger`**——防禦從「按著就好」變成一次讀招。遠程武器不能招架。
- [x] **敵種中世紀化＝正規 key + 對照表（使用者定案）**。
      正規 key（**新地圖只能用這組**，`ENEMY_TYPE_IDS`／編輯器工具列也只提供這組）：
      `levy 農兵 / footman 徒步兵 / footman_flank 包抄 / spearman 槍兵 / wolf 戰狼 /
       archer 弓手 / arbalest 弩手 / manatarms 披甲士 / serjeant 軍士 / civilian 平民 / dummy*`
      舊 key 靠 **`LEGACY_ENEMY_ALIAS`** 對照（`grunt→footman`、`dog→wolf`、`heavy_lmg→serjeant`、
      `heavy_shot→manatarms`、`civ→civilian`），`resolveEnemyType()` 轉換，且**單位身上一律存正規 key**
      （Hooks／統計／測試看到的都是新名字）。首次用到舊 key 會 console.warn 一次。
      **phase out 機制**：`legacyEnemyUsage()` 盤點進度、地圖標 `medieval: true` 即由 `_check.js` 強制
      只能用正規 key。目前 2/12 已中世紀化（arena、range），其餘 9 張舊圖待 Phase 4 手刻時汰換。
      等全部改完，把 `LEGACY_ENEMY_ALIAS` 整張刪掉即可。
- [x] **`EQUIPMENT` 中世紀化**（旅人斗篷/護手/箭袋/磨刀石/輕靴/聖物護符/鐵環襯裡）——
      舊的 scope/quickmag/extmag/stabil 是槍械配件，槍已刪；這組沒有地圖依賴，直接改 key 不必對照表。
- [x] **四槽位改成 1-4 直切**（使用者回饋：比 X 換手直覺）。試作場的主手輪盤讓位給槽位，改用 Tab／Shift+Tab。
- **驗收**：`node _check.js` 209/209。

**待辦（B 批）**
- [ ] **統一單位數值表**（使用者提案：扈從＝敵人）：把 hp/體幹/移速/傷害抽成一份共用表。
      **例外要保留**：耐力仍只有玩家操作的騎士管理（AI 不會用；壓力由體幹＋攻擊權承擔）。
- [ ] 魔法：投射系「武器」（火矢＝高 pierce 走同一彈道管線）+ `ABILITIES` CD 技（術/奇蹟）。
- [ ] 盾兵敵種（會格擋的敵人）＝需要 AI 會用盾，比較大工程。
- [ ] 休息關配裝 UI 接上四槽位（目前仍是舊的主/副武器兩列；roster 欄位已相容，只是 UI 還沒給副手/自由位）。

### Phase 2（原始規劃，保留對照）— 遠程副武裝與魔法
- [ ] 騎士遠程副武器切換（弓/弩/投槍），沿用原版瞄準/彈道/換槍管線（`weaponSwap`）。
- [ ] 魔法：投射系「武器」（火矢等）+ `ABILITIES` CD 技（術/奇蹟），低度奇幻不過火。
- [ ] `WEAPONS` / `ENEMY_TYPES` 全面中世紀化（長弓/十字弓/標槍/劍斧鎚槍/盾；徵召兵/披甲士/戰狼/弩手/農兵）。
- **驗收**：騎士能近↔遠切換；數種魔法/術可用；敵種中世紀化。

### Phase 3 — 友軍系統與指令層

**已完成：指令層 + 扈從移動 AI（提前做，因為「統一單位數值」必須先回答這題）**

問題（使用者提出）：敵人性格是為了「阻擋玩家」而設計的，變成我方會不會不適合一起行動？

結論：**性格其實可以共用，缺的是「參考框架」**。查 `scoreNode` 的四個權重，
`wRush`／`wFlank`／`wSight` 全都是**目標相對**（陣營中立），只有 `wExpose` 寫死了 `activePlayers()`。
真正缺的是：敵人 AI 的世界觀只有「我的目標在哪」，完全沒有「我該待在誰附近」。

解法＝**指令 = 錨點 + 繩長 + 交戰規則；性格 = 在繩子內怎麼站**（兩層正交）：

| 指令 | 錨點 | 繩長 | 交戰規則 |
|---|---|---|---|
| 跟隨（預設） | 騎士（動態） | 190 | 只打繩長內的敵人 |
| 守住此處 | 下令當下的座標 | 90 | 打進來的，不追 |
| 集中攻擊 | 指定的那隻敵人 | 340 | 全力打它（無視射程/姿態限制） |
| 先行撤離 | 撤離點 | — | 沿用既有 `EXTRACTING` |

- `scoreNode` 加**繩長懲罰項**＋`wExpose` 陣營中立化，就讓同一份性格在扈從身上成立。
- `ARCHETYPES` 加 `aiPersonality`：磐石→holder、先鋒→rusher、遊獵→flanker。
- **踩雷：`moveOnIdle:false` 是個從未被讀取的死旗標**——原版扈從根本不會移動，
  所以這不是微調而是「從靜態 overwatch 換成移動 AI」。
- **踩雷：近接「衝完最後一段」若對所有扈從一視同仁，會把性格整個蓋掉。**
  實測磐石(holder) 會站得比先鋒還前面（離敵 40px vs 99px），與「持盾固守」完全相反。
  改以 `wRush ≥ CONFIG.ai.closeInMinRush` 當門檻。修正後實測：
  磐石 離騎士26/離敵162、先鋒 離敵119偏角1°、遊獵 離敵77——分工讀得出來。
- **攻擊權改為「以目標為 key」的池**：原本是全域單一 Map（上限 2），扈從一旦也會近戰
  就會跟敵人搶同一組配額＝「全場同時只有 2 個人在打」。每個被攻擊的目標各有配額才對。
- **AI 近戰也加了友軍誤傷閘門**（揮擊扇形內有自己人就不揮），與遠程收火同一原則；
  **玩家的手動攻擊仍不受此限**。
- **指令輪盤**（使用者選：長按選單）：按住 `G` → 四向輪盤、滑鼠左右轉動選擇、放開下令。
  「守住／集中攻擊」以騎士準星指向決定位置／目標。
- **使用者定案：扈從沒有自保行為**（不會低血後撤），死生完全由指令負責。
  配套＝扈從狀態可讀性（頭頂指令標記、重傷閃爍警告、螢幕邊緣箭頭），
  否則體驗會變成「他死了而我沒注意到」而不是「我下錯指令」。

**待辦**
- [ ] 招募改寫：沿途潰兵/難民/傭兵、救出俘虜入隊（沿用 `makeRecruit` 與俘虜護送）。
- [ ] **輕量指令層**：跟隨 / 守住此處 / 集中攻擊目標 / 先行撤離（熱鍵；沿用原版 `F`＝撤離）。
- [ ] `ARCHETYPE` 中世紀化：磐石＝持盾守、先鋒＝陷陣、遊獵＝斥候散兵。
- [ ] 第三勢力中世紀化（盜匪/潰軍/民團/領主巡騎）與「助其而友軍化」的事件。
- [ ] 觀察「軍團 vs 孤騎」平衡風險（友軍越多越易變傍觀者）——先以創發＋數值調整處理，不預建專用系統。
- **驗收**：能沿途擴充隊伍並下達基本指令；友軍死亡永久生效。

### Phase 4 — 題材 / 敘事 / meta 重塑
- [ ] 路線圖節點與場景敘事改寫（焚村/林道/渡口/關隘；野營/鍛冶/路上遭遇）。
- [ ] 主選單 / 總部(HQ) / 結局改寫為歸鄉・贖罪主題（移除原間諜「沉睡者」敘事、The Patriot 致敬等）。
- [ ] 以編輯器手刻中世紀地圖；source/sink 經濟平衡。
- **驗收**：一條可玩的歸途路線，具開場—中段—終局敘事。

### Phase 5 — 打磨與（選配）架構
- [ ] 可讀性 / juice / 平衡調校（沿用 `TUNE` 面板）。
- [ ] **`CONFIG.juice` 的 `camKick` / `shakeFire` / `shakeHit` / `shakeKill` 目前全是 0**——原版把整條螢幕震動管線接好了卻沒開。Phase 1 近戰打擊感可直接從這裡拿甜頭。
- [ ] （選配）拆分 ES 模組以利維護（純 vanilla，不引入引擎）。
- [ ] （選配）發佈：Hugo 上架、於 `content/projects.md` 連結。

### Phase 6（候補，未排程）— 其他模式
- 原版的 `extraction_arcade/`（搜打撤 arcade 變體：寶物/滿意值撤退/獵人 FFA/本機排行榜）可作為**比武大會／生存者模式**的土台。目前未複製進本 repo（會與 Homeward 的 `index.html` 大幅分歧），需要時自 `darkbearlab/sleeper_roguelite` 取回。

---

## 開發守則（承襲原版設計憲章，因應「去附身」調整）

- **攝影機完全踏襲原版**，任何簡化不得退化瞄準/旋轉手感。
- **友軍永久死亡（down = dead）**：壓力前移到「預防與護送」。
- **追兵軟計時只是壓力調節，不是失敗畫面**；但**騎士死亡 = run 結束**是唯一硬敗北。
- **敵人不做空間尋路**，沿用手刻 waypoint 圖（reservation + hysteresis 必留）。
- **數值先求手感**，集中於檔頂 `CONFIG` / `TUNE`，先跑得動再調平衡。
- **陣營資料驅動**：新增勢力＝加一條 `FACTIONS` + spawn 指定 id，戰鬥/AI/繪製不改碼。
