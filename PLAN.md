# HOMEWARD — 分階段實作計畫

> 一句話定位：**戰敗騎士的歸鄉之路。你只操作騎士本人（近接為主、遠程為副），沿途收攏 AI 友軍；騎士陣亡即 run 結束。承襲 sleeper_roguelite 的單檔 Canvas2D 架構，攝影機完全沿用原版。**

本檔是開發脈絡與待辦。數值先求手感、集中於檔頂 `CONFIG` / `TUNE`。一步一步做，每階段有明確驗收。

---

## 技術基線（承襲原版，不更換引擎）

- **單一 `index.html`、原生 JS、Canvas 2D、零依賴零資產。** 已評估過 Phaser 等成熟引擎：引擎只能取代原版已完成的描繪/輸入層，無法提供真正的價值（戰術 waypoint AI、視界、撤離、meta 皆為引擎無關的自寫邏輯）。故沿用原版，不移植。
- **攝影機完全沿用原版**：旋轉跟隨 + 指標鎖定（FPS 式瞄準）。不退化為固定俯瞰。
- 保留原版資料驅動骨架：`CONFIG / WEAPONS / ABILITIES / EQUIPMENT / ITEMS / ARCHETYPES / PASSIVES / ENEMY_TYPES / FACTIONS / MAPS / ROADMAP` 與 `TUNE` 即時調整盤。

## 已確定的設計決策

1. **無附身**：全程只操控騎士本人。`controlledId` 固定為騎士。
2. **騎士死亡 = run 結束**（友軍死亡不直接結束 run）。
3. **近接為主、遠程副（弓/弩/魔法）可切換**；保留玩家的滑鼠瞄準技巧。
4. **友軍永久死亡（down = dead，同原版）**。
5. **輕量指令層**：跟隨 / 守住此處 / 集中攻擊目標 / 先行撤離。
6. **meta / 總部(HQ) / 結局敘事層** → 後置到 Phase 4，先完成「一段歸途」的可玩垂直切片。
7. **魔法（低度奇幻）** = 投射系「武器」（火矢等）+ `ABILITIES` CD 技（術/奇蹟）混用。

---

## 階段

### Phase 0 — 基線與去附身（土台）✅ 完成
- [x] 將原版 `index.html` / `editor.html` 置於本 repo 根目錄作為基線。
- [x] 移除附身/切換系統：`possess()` / `autoPossess()` / 數字鍵切換 / `CONFIG.switchHandoverMs` / `lastSwitchT` 全數移除；`controlledId` 只會是 `KNIGHT_ID(0)` 或 null。
- [x] 友軍恆為待機 AI 人格（`ARCHETYPE`）；騎士恆為唯一受控單位（`players[0]`）。
- [x] 勝負判定改主體：**騎士親自抵達撤離點 = 過關**；**騎士死亡 = run 結束**（`loseReason = '騎士陣亡'`）。
- [x] 遺物機制（原版機密檔案）以 `CONFIG.relicEnabled = false` 關閉，Phase 4 復活。
- [x] 確認保留無損：敵 AI、視界/迷霧、撤離、追兵軟計時、路線圖、休整/軍械庫/事件、陣營 FFA、攝影機。
- **驗收**：`node _check.js` 66/66 全綠（含全場景逐幀 update+draw、騎士抵達過關、騎士陣亡結束 run、扈從陣亡不結束、相機 `-π/2 − facing` 耦合未退化）。

**Phase 0 的設計決定（實作時定案，記錄備查）**
1. **騎士抵達撤離點時，尚在場的存活扈從隨之脫離**（不判定為損失）。理由：`idleTick` 的 `moveOnIdle:false` 代表扈從不會自己跟上，Phase 3 加「跟隨」指令之前若判損失＝每場全滅扈從；永久死亡只該由陣亡觸發。Phase 3 指令層完成後可再收緊。
2. **H = 騎士自走撤離**（自動導航＋滑鼠仍可轉視野，任一指令奪回）保留；**Shift+H 改為「命令扈從先行撤離」**（騎士不受影響）＝Phase 3「先行撤離」指令的雛形。
3. **E/Q 改為對全體扈從下姿態**（主動清場／防守）。原版是對「當前附身單位」下，去附身後騎士恆為手動、stance 對他無意義，否則變成死鍵。
4. **序章（第0關）的間諜敘事暫不動**（控制台「取得機密檔案」等），Phase 4 連同 HQ/結局一起重寫。

### Phase 1 — 近接戰鬥機制（手感核心）

> **開發方式（使用者定，2026-07-21）**：不在戰役裡做。先開一個**隔離的近戰試作場**（`?arena`，
> 目前是**開機預設**），關掉迷霧/時限/追兵/路線圖/序章/撤離判定，只留近接。手感調定後再把戰役接回來
> （屆時把檔尾 boot 的預設改回 `goMenu()`）。原本的主選單走 `?campaign`，或在試作場按 **M**。

- [x] **揮斬**：三段狀態機 WINDUP→ACTIVE→RECOVER，可 cleave 多目標。
- [x] **揮動軌跡**（使用者回饋後改）：不是「扇形整個推出去」，而是**刀刃在判定窗內真的移動**——
      `swingStyle:'sweep'` 由一側掃到另一側且**左右交替**（連揮＝→、←、→…）、`'thrust'` 刀尖沿正前方推出去。
      判定只吃「刀刃這一幀掃過的角度帶」（±`bladeHalfDeg` 刀刃厚度），不是整個扇形同時生效。
- [x] **盾格**：右鍵舉盾（持近接武器時取代望遠）；正面 `blockArcDeg` 內減傷 `blockResist`，移動/轉向變慢。
- [x] **招架（parry）**：舉盾後 `parryWindow` 內擋下＝完全格擋 + 攻擊者硬直。
- [x] **硬直**：`staggerT`（戰鎚命中 / 招架成功造成）——中斷揮擊、完全不能行動。
- [x] **擊退**：衰減速度而非瞬移＝仍吃 `collideWalls`，不會被推穿牆。
- [x] 節奏數值：前搖/判定窗/後搖/扇形角/觸及/cleave/擊退/硬直 → 全在 `WEAPONS` + `CONFIG.melee`，數值面板（`` ` ``）即時可調。
- [x] 近接武器差異化：劍(均衡・橫掃 100°) / 斧(慢・橫掃 160°・cleave 5) / 鎚(穿甲 0.95・大擊退・硬直) / **槍(突刺・reach 104・寬容僅 16°)** / 短劍(快・背刺 ×2)。
- [x] 盾＝`EQUIPMENT`（圓盾/鳶盾＝被動減傷 + 負重）。
- [x] **體幹（poise）** — 每單位一條會回復的體幹條，命中扣體幹；扣光＝**體幹破**（大硬直 + 受傷 ×1.6 處決窗 + 大幅擊退）。雜兵低（劍 3 擊）、重裝高。
- [x] **打斷** — 前搖中被命中＝這一刀直接被打斷；斧/鎚在判定窗中有 **hyperArmor**（免打斷、體幹傷害減半）＝重武器的身分。
- [x] **耐力（stamina）** — 揮擊/格擋/奔跑都要錢；歸零＝揮不動；**舉盾時耐力不足擋下 → 破防**。招架成功退還耐力。敵人不吃耐力（AI 不會管理，壓力改由體幹＋攻擊權承擔）。
- [x] **攻擊權** — 同時只有 `maxAttackers`(2) 隻近接敵能進攻，其餘在 `ringDist` 外繞行；揮完一刀必須退開 `tokenRest` 才能再拿＝進攻有節奏。
- [x] **攻擊預告 telegraph**（使用者定）— AI 出手前放出「會打到哪」的形狀，由中心**擴散填滿到末端**，
      **填滿的瞬間＝攻擊落下的瞬間**；只對玩家視線內的敵人顯示（背後偷襲仍然成立）。
      `telegraphOf(u)` 是通用接縫。**近接＝扇形、遠程＝長方形彈道走廊（會被牆截斷）**，兩者共用同一套填滿邏輯。
      為了讓遠程「有東西可預告」，AI 遠程加了瞄準蓄力期 `CONFIG.enemy.aimTime`（武器可覆寫 `aimTime`）——
      原版是看到就射、沒有前搖。**玩家手動射擊不受此限**（仍即時）；扈從與敵人對稱都要瞄。
      預告對象＝**騎士以外的所有單位**（扈從也畫：FF 開著，要看得到別站進自己人的火線）。
- [x] **友軍誤傷（全面開啟）** — 原版沒有，本作所有攻擊都會誤傷。關鍵區分：**索敵**仍走 `hostile()`
      （沒有人會「瞄準」自己人），但**傷害判定**吃到誰就是誰。斧的 cleave 會砍到自己的扈從＝設計上的張力。
- [x] **裝甲/穿甲（使用者定案 2026-07-21）** — 近戰**維持原版全機率制**（有效擋格率 = armor × (1−pierce)，
      繞背無視）：被彈開＝這刀白揮，「必須換鎚」的壓力最大。三個新決定：
      1. **裝甲彈開的那一擊，體幹傷害 ×`armorPoiseMul`(1.7)**——刀滑開但衝擊還在＝裝甲讓你
         「砍不動他，但更容易把他砸崩」。重裝的解法是鈍器，不是硬砍。
      2. **騎士有裝甲槽（`ARMORS` 資料表）**：布衣/厚襖/鎖子甲/胸甲/板甲，走與重裝敵人完全相同的
         擋格機制；代價＝移速、耐力回復、負重（奔跑）。穿越重越挨得住、但越快力竭＝安全 vs 續戰力。
      3. **盾格擋下的攻擊不再擲甲**（盾攔住的刀沒碰到甲，不做雙重防禦）。
      順帶修正：裝甲擲骰原本排在體幹**之後**（意外而非設計）；重排後彈開的擊退/體幹在同一處結算。
- [x] **穿甲軸的武器分工（近戰+遠程）** — 原版 rifle(0.5)/DMR(0.9) 的位置由中世紀武器接手：
      近戰＝鎚 0.95（近戰的 DMR）＞槍 0.55 ＞劍 0.35＞斧 0.30＞短劍 0.20；
      遠程新增 **長弓 0.15（反人員）/十字弓 0.85（破甲）**，沿用既有射擊管線（magazine 1 + reload＝搭箭/絞弦）。
      特定魔法（火矢等）Phase 2 一樣走此管線、給高 pierce 即可。
- [x] **Hooks 事件匯流排（roguelite build 的地基，使用者要求）** — 之後的聖物/祝福/詛咒全部走
      「訂閱事件 + 改 ctx」，引擎不再為每個效果開洞。已接線的事件（詳見 index.html 檔頂 Hooks 註解）：
      `attackStart / attackHit / hurt / blockHit / parry / armorDeflect / interrupt /
       poiseLow / poiseBroken / hpLow / stamEmpty / beforeDeath(可攔截＝復活通道) / death /
       kill(含 elite/boss 旗標與近戰/遠程歸屬) / missionStart / missionEnd`
      - 門檻類（hpLow/poiseLow）＝跌破觸發一次、回到門檻上重新武裝（`CONFIG.hooks`）。
      - handler 全包 try/catch＝一個壞掉的內容不拖垮戰鬥。
      - 「每場一次」狀態放 `Hooks.mission`（missionStart 自動清空）。
      - **RELICS 目錄 + 示範聖物「不屈」**（每場一次：致死傷害改留 1 HP）＝復活通道已打通；
        試作場 V 鍵切換。`knightRelics` run 中撿、`initRun` 清空。
      - `ENEMY_TYPES` 新增 `elite`（heavy 系）/`boss`（Phase 4）標記供 kill 事件歸類。
- [x] **試作場分場（使用者定）** — `ARENA_MODES` 登錄表：一個場地＝一條資料（地圖 + 配置 + 預設武器）。
      目前兩場：近戰（假人+劍兵）/ 射擊（上半標靶區 300–900 距離檔、下半對射區弓手×2 弩手×1）。
      **入口選單 scene `ARENASEL`＝開機第一個畫面**（自動列出所有 ARENA_MODES，含 desc/detail）；
      進場後 Esc 回選單、U 直接切下一個、`?arena=<key>` 直達。
      **之後的 gimmick 測試場＝加一條 ARENA_MODES + 手刻一張小圖**，選單與切換都自動生效、不改碼。
      新敵種 `archer`（長弓）/`arbalest`（十字弓）＝Phase 2 中世紀化直接沿用。
      踩雷：**假人與敵人同陣營，不能擺在對射走廊上**——會觸發 AI 的「射線被自己人擋住＝收火」，
      弓手永遠不放箭（實測抓到，故標靶區/對射區分上下半）。
- [x] **彈道速度已調** — `CONFIG.juice.bulletSpeedMul` 4 → **1.5**（原版 4 是為了讓槍戰接近 hitscan）。
      長弓實際 960px/s（飛 500px 0.52s）、十字弓 1380px/s（0.36s）＝看得見在飛、能預判走位。
      碰撞本來就是掃描式判定，放慢彈速不會漏判。試作場遠程參數表有列實際彈速與飛行時間。
- [ ] **待你試玩後調數值**——這是本階段真正的工作，程式只是把旋鈕接出來。
- [ ] 進階（後續）：突刺（拉近距離）、怯步、側步/迴避（先不做，若被圍毆時覺得沒有出口再加）。

**「戰鬥只是換血」的解法與實測（2026-07-21）**
根因＝**攻擊沒有成本、被打沒有代價**，所以雙方最佳策略都是「一直揮」。用兩條互相獨立的資源補：
耐力管「攻擊的成本」、體幹管「被打的代價」；再加攻擊權讓 1 對多 可讀。

實測（headless 模擬 1v3，騎士 vs 3 劍兵，各跑 40s）：

| 打法 | 結果 | 剩餘 HP | 招架 |
|---|---|---|---|
| 無腦一直揮 | 清場 | 52/100 | 0 |
| 會擋、會招架 | 清場 | **100/100** | 3 |

同樣清場，防守打法毫髮無傷＝**激勵梯度正確**。

**過程中抓到的失調（都是實測才看得出來的）**
- 擊退位移 ＝ `knock / knockDecay`，原本劍只有 **16.7px**，比兩個單位半徑和（24px）還短＝物理上不可能製造間合。已把 `knockDecay` 降到 6、各武器 knock 大幅提高。
- **短劍原本是 DPS 之王**（95，最高），與「快而弱＋背刺」的定位相反。已下修。
- 敵我共用同一份 `WEAPONS`，但這是「英雄 vs 群體」的遊戲：不下修的話騎士 **4 刀就被砍死**（100hp ÷ 30dmg），而清 3 隻雜兵要 9 刀＝**數學上必輸**。新增 `CONFIG.enemy.meleeDmgMul`(0.45)。
- `bladeHalfDeg` 9° 太薄、`swingTurnMul` 0.25 太死 → 對走動中的目標命中率極低（實測 39 揮只中 5）。已放寬到 14° / 0.4。
- `ringDist`(115) > 長劍觸及(78)＝等待中的敵人站在你打不到的地方。這是**刻意**的（要你自己踏進去），但代表玩家必須主動逼近，不能站著揮。

**友軍誤傷的必要配套：`CONFIG.friendlyFire.aiHoldFire`**
只打開 FF 而不管 AI，敵人會互相掃射把自己打光。AI 開火前必須檢查射線上有沒有自己人擋著。
實測（8 名遠程步兵擠成兩排對玩家開火 30s／扈從與敵人之間夾著騎士 30s）：

| | 敵人死於自己人 | 騎士被扈從誤殺 |
|---|---|---|
| `aiHoldFire` 關 | **6 / 8** | **是** |
| `aiHoldFire` 開 | **0 / 8** | 否（HP 100/100） |

- 只套**遠程**。近接不套——短距離掃擊如果也要求「扇形內不能有自己人」，AI 會完全不敢動手。
- **玩家的手動攻擊永遠不受此限**：自己的刀自己負責。
- **驗收**：`node _check.js` 94/94；手感由使用者在試作場實測認定。

**實作要點（供後續接手）**
- 揮斬掛在 **`fireUnit` 的最前面**（`if (W.melee) return startSwing(...)`），所以**玩家手動 / 扈從 overwatch / 敵人 AI 三條既有路徑全部自動共用**，沒有玩家專用碼。Phase 2 敵種中世紀化只要把 `ENEMY_TYPES.weaponId` 換成近接武器即可。
- `enemyTick` 的「衝完最後一段」改吃 `e.weapon.melee`（原本只看 `range ≤ meleeApproach`，長槍 reach 104 會被卡住）。
- `damageUnit` 加第 5 參 `opts {melee, attacker, knock, stagger}` 並**回傳是否被格擋**。
- 可讀性：前搖時刀停在起始側 + 淡淡的整段預告弧（**看得出要從哪邊掃過來**）；判定窗畫「已掃過的尾跡 + 前緣刀身」（**所見即所中**）；突刺畫一條推出去的刀 + 刀尖光點；舉盾畫護弧，招架窗內更亮。
- **踩雷：`rectWalls(x1,y1,x2,y2)` 吃的是對角座標，不是 `(x,y,w,h)`。** 第一版試作場誤當後者，`rectWalls(520,450,60,60)` 變成一個 460×390 的大方框，把騎士出生點整個封在裡面（實測：0/3 敵人與騎士之間無阻隔）。`_check.js` 已加兩道防線：柱邊長度 ≤80px、以及「至少一名敵人與騎士之間無牆」——注意**單看距離會空過**（敵人會貼到牆的另一側，距離看起來很近但永遠打不到）。

### Phase 2 — 中世紀化・四槽位・魔法

**已完成（A 批）**
- [x] **武器全面中世紀化**：槍械全數刪除。近接＝短刀(底線)/短劍/長劍/彎刀/戰鎚(1手)、雙手斧/長槍(2手)；
      遠程＝長弓/十字弓(2手)、**投槍(1手＝唯一能搭盾的投射武器)**；敵人專屬＝獠牙/木棍。
      終局解鎖 Siren（The Patriot 致敬）→ **喪鐘**（雙手大劍，`loud`＝每擊全場聽得見，放棄潛行換威力）。
- [x] **`hands` 欄位**（1/2）＋ **`OFFHANDS` 副手表**：小圓盾(窄護弧但招架窗最寬) / 圓盾(均衡) / 鳶盾(護弧最寬減傷最高但耗耐力、難招架)。
- [x] **四槽位**（使用者提案）：槽 1/2＝手部 set `{main, off}`，槽 3/4＝自由位（道具充能制 / 技能 CD 制）。
      換 set 要時間（沿用 `CONFIG.weaponSwap`），**各 set 自帶彈藥/裝填狀態**（換手不會弄丟裝填進度）。
      相容做法：`u.weapon*` 仍是「當前主手」的鏡像，既有戰鬥碼一行沒改。
- [x] **格擋必須有盾**（補掉「雙手斧與劍盾防禦力完全一樣」的漏洞）；
      **無盾＝武器招架**（使用者定案）：點按右鍵、窗僅 `wpWindow`（比盾窄）、成功回報與盾招架同等，
      **但空招會自己硬直 `wpFailStagger`**——防禦從「按著就好」變成一次讀招。遠程武器不能招架。
- [x] **敵種中世紀化＝正規 key + 對照表（使用者定案）**。
      正規 key（**新地圖只能用這組**，`ENEMY_TYPE_IDS`／編輯器工具列也只提供這組）：
      `levy 農兵 / footman 徒步兵 / footman_flank 包抄 / spearman 槍兵 / wolf 戰狼 /
       archer 弓手 / arbalest 弩手 / manatarms 披甲士 / serjeant 軍士 / civilian 平民 / dummy*`
      舊 key 靠 **`LEGACY_ENEMY_ALIAS`** 對照（`grunt→footman`、`dog→wolf`、`heavy_lmg→serjeant`、
      `heavy_shot→manatarms`、`civ→civilian`），`resolveEnemyType()` 轉換，且**單位身上一律存正規 key**
      （Hooks／統計／測試看到的都是新名字）。首次用到舊 key 會 console.warn 一次。
      **phase out 機制**：`legacyEnemyUsage()` 盤點進度、地圖標 `medieval: true` 即由 `_check.js` 強制
      只能用正規 key。目前 2/12 已中世紀化（arena、range），其餘 9 張舊圖待 Phase 4 手刻時汰換。
      等全部改完，把 `LEGACY_ENEMY_ALIAS` 整張刪掉即可。
- [x] **`EQUIPMENT` 中世紀化**（旅人斗篷/護手/箭袋/磨刀石/輕靴/聖物護符/鐵環襯裡）——
      舊的 scope/quickmag/extmag/stabil 是槍械配件，槍已刪；這組沒有地圖依賴，直接改 key 不必對照表。
- [x] **四槽位改成 1-4 直切**（使用者回饋：比 X 換手直覺）。試作場的主手輪盤讓位給槽位，改用 Tab／Shift+Tab。
- **驗收**：`node _check.js` 209/209。

**已完成：統一單位數值表（使用者提案：扈從＝敵人）**

`UNIT_CLASSES` 是**唯一**的身體來源：hp / 體幹 / 移速 / 半徑 / 甲。敵我兩邊都從這裡拿，
所以一個 `footman` 站在哪一邊都是同一副身體。

- `ENEMY_TYPES` 降級成**角色表**（武器 / 性格 / 外觀 / 巡邏 / 菁英標記），
  用 `enemyType(cls, role)` 合併——`TYPE.hp` 之類的下游讀取一行都沒改。
- 扈從走 roster 的 `classId`（缺就由原型給預設）；招募時**兵種與原型各擲一次**，
  身體與行為分開＝會出現「體格像農兵但打法是先鋒」的人，招到誰真的有差。
- 騎士固定 `knight` 級（100/100），不吃兵種削弱；`spdOf()` 讓騎士仍讀 `CONFIG.moveSpeed`＝數值面板可即時調。
- 改動幅度：扈從 hp 100→70(或弓手 60)、體幹 **100→45**、移速 110→105。之前**每個扈從都是騎士級體幹**。

**這次重構掀出兩個結構性 bug（都是統一之後才看得見）**

1. **扈從有耐力但沒有管理耐力的腦**。文件寫的是「只有玩家操作的騎士管理耐力」，
   但扈從實際上 `stamMax:100` 且照常消耗——AI 不會退出來回氣，打到見底就變沙包。
   舊版 100 體幹把這件事遮住了，體幹一削到 45 才暴露（實測混戰中扈從最低耐力剩 2/100）。
   → 扈從改 `stamMax:0`，所有 `if (u.stamMax)` 閘門短路，行為與敵人完全一致。

2. **固守型近戰扈從永遠不進入攻擊距離**——`closeInMinRush` 的代價。
   當初加這個閘門是為了修「磐石站得比先鋒還前面」的倒錯，但它把「要不要打」也一起關掉了：
   敵人走 `enemyTick`，近戰**無條件**衝完最後一段；扈從要 `wRush ≥ 400` 才貼上去。
   **實測同兵種同武器 1v1：磐石 0 勝 24 敗**（站著被砍死），先鋒 20 勝 4 敗。
   → 改成**性格決定「站多近」而不是「要不要打」**：近戰一律進到自己打得到的距離，
   進取型貼到接觸、固守型停在 `reach × holderStandoffFrac`。

**實測（釘住靶，量的是站位而非擊退追擊）**

| | 穩定後平均離敵 | 最近 | 1v1 同兵種同武器 |
|---|---|---|---|
| 磐石（wRush 200） | 55px | 49px | 修正前 **0 勝 24 敗** → 修正後 24 勝 0 敗 |
| 先鋒（wRush 700） | 39px | 23px | 20 勝 4 敗 |

武器 reach 58——磐石剛好停在打得到的邊緣，先鋒壓到接觸。**兩個都打得到人，但先鋒站得更前面。**

> 量測踩到的坑：一開始用「結束時的距離」和「平均距離」都得到倒錯的結果（先鋒 178px 比磐石 131px 還遠），
> 因為戰鎚擊退 560 會把目標轟開，先鋒整段時間都在追。**把靶釘住**才量得到站位本身。

**已完成：術（魔法/投擲）— 一份內容、兩種載具（使用者提案）**

使用者的洞見比「做一套魔法系統」更根本：**內容只有一份，資源模型有兩種**——
卷軸（撿到的）與習得（學會的），而「其實可能是本來的投擲武器們」。

- `ARTS` ＝ 自由槽位(3/4)的**唯一**內容表。只描述「效果是什麼」，不描述「你憑什麼用它」。
  `kind` 直接對應既有管線（`projectile`→彈道／`thrown`→投擲／`self`→自身）＝沒有新的戰鬥碼。
- `ITEMS`／`ABILITIES` 降為 ARTS 的**投影**，戰利品池/倉庫/休息關 UI/沙盒面板約 85 處一行未改。
- 舊 key 走 `LEGACY_ART_ALIAS` + `resolveArt()`（同 `LEGACY_ENEMY_ALIAS` 的 phase out 做法）。

| 載具 | 次數 | CD | 前搖 | 耐力 | 被打斷 |
|---|---|---|---|---|---|
| **卷軸** `sl.type==='item'` | 有限 | 無 | 無（即時） | 不吃 | **不會** ＝ 救命按鈕 |
| **習得** `sl.type==='ability'` | 無限 | 有 | 有 | 吃 | **會** ＝ 可持續的工具 |

★ **載具改變成本，不改威力**。同一發火矢兩種載具打起來一樣痛，所以「找到已學會法術的卷軸」
仍然有價值（即時且打不斷是習得給不了的），而且一個術只有一組數值要調。
被打斷時**不進 CD 也不扣耐力**——成本在 `tickCast` 收尾才付。

`learnable:false` ＝只有卷軸那一欄（陶罐/藥/箭袋本來就學不會），讓投擲道具與魔法共用一張表而不彆扭。

**導師節點（使用者定案：複合式）**
- 路線圖新增 `TUTOR` 類型。節點類型本來就在生成時決定並畫出來，所以**分岔時就看得見「這條路上有導師」**
  （這是使用者問的「可否預先偵查」——答案是本來就可以，不需要新機制）。
- 燒 `ARTS[x].scrollsToLearn` 張卷軸換永久習得（**各術不同＝旋鈕**：癒手/包紮/火矢 2、藥膏/聖光 3、聖療 4）。
- 一張＝一個充能。扣卷軸會跨多個倉庫堆疊扣，且**學不成時不會偷扣**。

**順手修掉三個既有問題**
1. **CD 存在單位上（`u.abilityCd`）＝兩個習得的術共用同一條 CD**。改存槽位 `sl.cd`。
2. **`useAbility`/`useItem` 都沒有 return**，而呼叫端寫 `const r = useX(u); if (!r) { 還原 }`
   → `r` 恆為 `undefined` ＝**每次都還原**。道具因為先寫回 `sl.count` 才僥倖正確。
   （與先前 `damageUnit` 沒有 return 同一類。）兩者現在只是 `castArt` 的別名，單一路徑。
3. 施法與揮擊/舉盾互斥；硬直與體幹破都會中斷施法。

**實測**：火矢對披甲士被彈開 7.0%，理論值 `armor 0.85 × (1 − pierce 0.9) = 8.5%`。

> 量測踩到兩個坑，**都是既有機制正確運作而不是 bug**：靶背對射手時吃背刺 ×2 且**無視裝甲**
> （量穿甲要讓靶面向你）、靶還在 IDLE 時吃 `surprise` ×1.5（要先進 ENGAGED）。
> 兩次都是先看到「數字剛好是 2 倍 / 1.5 倍」才回頭查出原因——整數倍的偏差通常是機制不是雜訊。

**待辦（B 批）**
- [ ] 魔法：投射系「武器」（火矢＝高 pierce 走同一彈道管線）+ `ABILITIES` CD 技（術/奇蹟）。
- [x] **盾兵敵種（使用者定案：不做預判、不做 AI）**——原本估「比較大工程」，使用者一句話把它變成小改動：
      「被攻擊後他沒有體幹崩掉就直接舉盾，一小段時間後放開即可，應該是承受傷害時的鉤子就能做了」。
      格擋機制本來就是陣營中立的（只看 `u.blocking`/`u.blockT`），只有 `shieldOf` 綁在玩家槽位上 →
      改成 fallback 到 `u.offId`，敵人就能持盾。反射式舉盾寫在 `damageUnit` 的體幹段（與打斷同一處）。
      **刻意不給招架窗**：招架是讀招、是玩家的技巧；讓純反射的 AI 每次挨打就白送一次招架
      （反推體幹＋硬直攻擊者），近戰打盾兵會變成純懲罰而不是攻防。
      敵人 `stamMax:0` ＝格擋不用付耐力，所以**時長就是它的成本**（舉 1.1s / 冷卻 0.5s）。

      **實測（6 刀、一律正面、各 40 場）——盾兵把正確的節奏顛倒過來**

      | | 連砍（0.45s 間隔） | 等他放下（1.75s） | 等待的效益 |
      |---|---|---|---|
      | 徒步兵 | 234 傷 | 216 傷 | **−8%**（等反而更差） |
      | **盾兵** | 171 傷 | 213 傷 | **+25%** |

      逐刀看得更清楚：第 1 刀 30 傷觸發舉盾 → 第 2、3 刀砍在盾上只有 **7.5 傷且體幹只扣一半**
      → 盾放下後才吃到 48 傷的處決窗。**盾把體幹破往後推了兩刀。**

      > 一開始我寫的是「徒步兵節奏無差」，測試因此會飄。實測 40 場後才發現**等待對一般敵人是
      > 負收益**——間隔拉長讓對方的體幹回復、破防次數變少。所以真正的命題不是「盾兵懲罰亂按」，
      > 而是**「盾兵讓原本正確的打法變成錯的」**：對一般敵人該壓著打，對盾兵該等。
      > 測試也改成驗這個**符號翻轉**而不是幅度（幅度會隨亂數浮動）。

      > 量測踩到的坑（同一個坑第二次）：敵人每幀會**轉身面向騎士**，所以固定寫死的 `fromAng`
      > 很快就不是它的正面了。第一版量到「盾舉著卻吃滿額傷害」，其實是打到側背（×2 且無視裝甲）。
      > 要量正面/背面就得**每幀把靶的朝向釘住**。
- [x] **休息關配裝 UI 接上四槽位**。動手才發現不只是 UI：**盾與甲根本不在戰利品經濟裡**——
      `OFFHANDS`／`ARMORS` 早就有 `name`/`rarity`（當初就照可掉落的東西設計），但從來沒接線，
      所以戰役裡永遠拿不到盾、也換不了甲，副手格會是個永遠空著的裝飾。
      → 先讓它們變成拿得到的東西（`kind:'off'`/`'armor'` 進倉庫、軍械庫三選一、搜刮、事件產出），
      再改 UI。配裝卡從 5 列變成 7 列（ch 176→232）：

      ```
      槽1  長劍 重1.5              ▸倉
      └副  圓盾 護弧120° 減傷75%   ▸倉
           彈 ∞（底線/近戰）
      槽2  長弓 重2.0              ▸倉
      └副  雙手武器（不能帶盾＝不能格擋）
           彈 24  儲備 60      − +
      槽3 卷軸 火矢 ×3             ▸倉
      槽4 習得 癒手（CD 8s）        ▸倉
      甲   鎖子甲  擋35% 速95% 耐回90%  ▸倉
      裝備 旅人斗篷                ▸倉
      ```

      雙手武器的副手格**顯示「雙手武器（不能帶盾）」而不是空白**——空白會讓玩家以為是自己沒裝。
      脫甲＝回到布衣而不是「沒有甲」，且**換甲時不會在倉庫裡生出一件布衣**（布衣是起始狀態不是戰利品）。

      > 順帶：火矢的穿甲測試原本容忍度只有 3σ（200 次取樣、理論 8.5%、抓 ±6%），
      > 大約每 20 輪飄一次。已放寬到 4σ——**會飄的測試比沒有測試更糟**。

### Phase 2（原始規劃，保留對照）— 遠程副武裝與魔法
- [ ] 騎士遠程副武器切換（弓/弩/投槍），沿用原版瞄準/彈道/換槍管線（`weaponSwap`）。
- [ ] 魔法：投射系「武器」（火矢等）+ `ABILITIES` CD 技（術/奇蹟），低度奇幻不過火。
- [ ] `WEAPONS` / `ENEMY_TYPES` 全面中世紀化（長弓/十字弓/標槍/劍斧鎚槍/盾；徵召兵/披甲士/戰狼/弩手/農兵）。
- **驗收**：騎士能近↔遠切換；數種魔法/術可用；敵種中世紀化。

### Phase 3 — 友軍系統與指令層

**已完成：指令層 + 扈從移動 AI（提前做，因為「統一單位數值」必須先回答這題）**

問題（使用者提出）：敵人性格是為了「阻擋玩家」而設計的，變成我方會不會不適合一起行動？

結論：**性格其實可以共用，缺的是「參考框架」**。查 `scoreNode` 的四個權重，
`wRush`／`wFlank`／`wSight` 全都是**目標相對**（陣營中立），只有 `wExpose` 寫死了 `activePlayers()`。
真正缺的是：敵人 AI 的世界觀只有「我的目標在哪」，完全沒有「我該待在誰附近」。

解法＝**指令 = 錨點 + 繩長 + 交戰規則；性格 = 在繩子內怎麼站**（兩層正交）：

| 指令 | 錨點 | 繩長 | 交戰規則 |
|---|---|---|---|
| 跟隨（預設） | 騎士（動態） | 190 | 只打繩長內的敵人 |
| 守住此處 | 下令當下的座標 | 90 | 打進來的，不追 |
| 集中攻擊 | 指定的那隻敵人 | 340 | 全力打它（無視射程/姿態限制） |
| 先行撤離 | 撤離點 | — | 沿用既有 `EXTRACTING` |

- `scoreNode` 加**繩長懲罰項**＋`wExpose` 陣營中立化，就讓同一份性格在扈從身上成立。
- `ARCHETYPES` 加 `aiPersonality`：磐石→holder、先鋒→rusher、遊獵→flanker。
- **踩雷：`moveOnIdle:false` 是個從未被讀取的死旗標**——原版扈從根本不會移動，
  所以這不是微調而是「從靜態 overwatch 換成移動 AI」。
- **踩雷：近接「衝完最後一段」若對所有扈從一視同仁，會把性格整個蓋掉。**
  實測磐石(holder) 會站得比先鋒還前面（離敵 40px vs 99px），與「持盾固守」完全相反。
  改以 `wRush ≥ CONFIG.ai.closeInMinRush` 當門檻。修正後實測：
  磐石 離騎士26/離敵162、先鋒 離敵119偏角1°、遊獵 離敵77——分工讀得出來。
- **攻擊權改為「以目標為 key」的池**：原本是全域單一 Map（上限 2），扈從一旦也會近戰
  就會跟敵人搶同一組配額＝「全場同時只有 2 個人在打」。每個被攻擊的目標各有配額才對。
- **AI 近戰也加了友軍誤傷閘門**（揮擊扇形內有自己人就不揮），與遠程收火同一原則；
  **玩家的手動攻擊仍不受此限**。
- **指令輪盤**（使用者選：長按選單）：按住 `G` → 四向輪盤、滑鼠左右轉動選擇、放開下令。
  「守住／集中攻擊」以騎士準星指向決定位置／目標。
- **使用者定案：扈從沒有自保行為**（不會低血後撤），死生完全由指令負責。
  配套＝扈從狀態可讀性（頭頂指令標記、重傷閃爍警告、螢幕邊緣箭頭），
  否則體驗會變成「他死了而我沒注意到」而不是「我下錯指令」。

**已完成：陣營士氣（唯一指標）— 使用者提案，取代第一版的個別士氣**

第一版是每個單位一條士氣。使用者提出改成**陣營士氣當唯一指標**，單位只持有**崩潰門檻**——
這比個別士氣好，理由不只是簡單：

1. **這才是中世紀戰場的模型**——會戰極少打到全滅，一方崩了就結束。
2. **一個數字取代 N 條旗標**：兩條（我方／敵方）掃一眼就知道戰局。
3. **崩潰變成連鎖而非各自跳水**：門檻不同 → 陣營士氣下滑會產生**序列**
   （農兵先垮 → 垮掉又壓低士氣 → 徒步兵跟著垮 → 連披甲士都崩）＝雪崩。
4. **「歸零＝擊潰」是正式的勝利條件**，不是湊巧的副產品。

- 門檻＝**純兵種**（使用者定案，不吃 HP/被圍等局部修正），且是**「佔自己那半的 %」**（見下節拉鋸）：
  `BREAK_PCT` 農兵 90 ＞ 弓手 70 ＞ 徒步兵 55 ＞ 披甲士/軍士 20（數字越低越晚崩）；
  我方 `ARCH_BREAK_PCT` 磐石 35 ＜ 先鋒 55 ＜ 遊獵 70。
- **所有扣分以「佔全軍的比重」計**（`deathFrac`/`breakFrac`/`damageFrac`），
  規模因此自動有意義：20 人的軍隊死 1 個遠不如 5 人小隊死 1 個動搖。
- 敵方歸零 → `fieldBrokenFor`＝**本關已勝、但場景不結束**（使用者選的「掃蕩時間」）：
  追兵停召、全軍潰逃，你可以搜刮/救人/追殺，自己決定何時從撤離點離開。
- 我方歸零 → 扈從全潰逃、**騎士照常**（不算失敗；騎士陣亡仍是唯一硬敗北）。
- 騎士光環改為**「聚攏度 → 陣營士氣回復」**（門檻既然是純兵種，光環改以聚攏影響那條唯一指標，
  仍然是玩家把部隊拉回來的手段）。
- HUD：畫面正上方兩條對峙的士氣條，**上面有各兵種崩潰門檻的刻度**＝看得出「再壓一點誰會垮」。

**兩個實測才抓到的問題**
1. **絕對值扣分會讓仗在 2.3 秒結束**：7 人隊死一個扣 14%、6 個農兵同時崩再扣 48%。→ 改成比重制。
2. **溢出傷害照全額扣士氣**：對 1 HP 的目標打 999 傷，士氣就掉 999×係數。
   → `damageUnit` 現在把 `dealt` 夾在剩餘 HP 內（`raw` 另外保留），
   所有吃「每點傷害」的效果（士氣、將來的吸血/統計）都不會被 overkill 灌爆。

**實測（孤騎 vs 6 農兵 + 1 披甲士）**

| 打法 | 耗時 | 擊殺 | 騎士剩血 |
|---|---|---|---|
| 打最近的 | 8.6s | 6 | 59/100 |
| **優先殺菁英** | **3.9s** | **5** | **84/100** |

士氣軌跡 100 → 83 → 55 → 38 → 6 → 潰散，雪崩看得見。

**已完成：拉鋸條 + 氣勢（使用者提案的第二次重構）**

使用者要的畫面是：**「敵人士氣太高，導致開戰前戰意就喪失」**。絕對士氣做不到這件事——
開場雙方都滿值，你不可能一開打就怕。所以改成**一條拉鋸**：

- **戰局線**`moraleLine()` = `50 + (我方 − 敵方)/2`，0..100，50＝均勢。**只吃差值**。
- **門檻改成「佔自己那半的 %」**：農兵 90% ＝ 線被推到 45 以下就垮；披甲士 20% ＝ 推到 10 才垮。
- **氣勢（`grantMomentum`）**：事件（首殺／斬將／招架）給予的**暫時額度，線性衰退到 0**，
  **只推戰局線、不動絕對士氣**（使用者定案：不做反撲、退盡後沒有永久受創）。
  這就是「開戰前戰意喪失」的接縫——伏擊給敵方 +60 氣勢，我方脆兵開場即崩。

**判準從加法改成乘法（模擬逼出來的）**

原本打算用 `relWeight` 線性混合絕對與相對，模擬後發現**結構性矛盾**：
`relWeight` 要 ≥0.85 氣勢才推得動門檻，但那個權重下「雙方消耗到 20/20」的戰意是 42.5＝沒人崩。
改成乘法就同時成立：

```
戰意 = 戰局線 × (絕對士氣/上限)^absExp        // absExp = 1.0
```
> **戰局位置 × 剩餘實力**。線說「誰佔上風」，指數說「還剩多少本錢」。

`_probe.js` 掃 `absExp ∈ {0, 0.7, 1.0, 1.5}` × 五種局面，**只有 absExp=0（純拉鋸）過不了**
「雙方殘破」那格（線還在 50，沒人崩）——這正是加法版失敗的同一個洞。取 1.0（數字最好讀）：

| 局面 | 戰局線 | 戰意 | 結果 |
|---|---|---|---|
| 勢均力敵 100/100 | 50 | 50.0 | 沒人崩 ✓ |
| **被伏擊**（敵氣勢 +60） | 20 | 20.0 | 先鋒・遊獵崩，**磐石不動** ✓ |
| 雙方殘破 20/20 | 50 | 10.0 | 都撐不住，弱的先崩 ✓ |
| 你打順了 80/20 | 80 | 64.0 | 穩 ✓ |

氣勢衰退的實測弧線（伏擊 +60 / 15s）：`0s 崩先鋒+遊獵 → 4s 只剩遊獵 → 8s 全回穩`。
**捱過開場那一下，隊伍會自己站回來**——這正是「伏擊」該有的體感。斬將可立刻推回 +21。

**又一個實測才抓到的坑**
- `topFoeFaction` 原本掃 `Object.keys(FACTIONS)`＝**登錄表**，把這張圖根本沒出場的 `rioter`
  也算成對手，戰局線莫名其妙歪掉（20/20 應該是 50，實際算成 10）。
  → 改成只看 `presentFactions()`（場上有活單位的陣營）。**登錄表不是出席表。**

**待辦**
- [ ] 招募改寫：沿途潰兵/難民/傭兵、救出俘虜入隊（沿用 `makeRecruit` 與俘虜護送）。
- [ ] **輕量指令層**：跟隨 / 守住此處 / 集中攻擊目標 / 先行撤離（熱鍵；沿用原版 `F`＝撤離）。
- [ ] `ARCHETYPE` 中世紀化：磐石＝持盾守、先鋒＝陷陣、遊獵＝斥候散兵。
- [ ] 第三勢力中世紀化（盜匪/潰軍/民團/領主巡騎）與「助其而友軍化」的事件。
- [ ] 觀察「軍團 vs 孤騎」平衡風險（友軍越多越易變傍觀者）——先以創發＋數值調整處理，不預建專用系統。
- **驗收**：能沿途擴充隊伍並下達基本指令；友軍死亡永久生效。

### Phase 2.5 — 舊地圖中世紀化（完成）

使用者定案：先把舊地圖全部中世紀化，**這樣至少能玩到完整的戰役**；事件地圖擱置，
開場與終局地圖也擱置、**直接進 roadmap**。

**六張路線圖地圖各自有一個戰術課題**（不只是機械換名——新兵種要真的排進去才算數）

| 地圖 | 課題 | 敵種 |
|---|---|---|
| **荒庭**（原 開闊庭院） | 開闊地要用掩體島 | 農兵×2・弓手・披甲士 |
| **關隘**（原 門禁設施） | 門是節奏：開門即撲 | 徒步兵・戰狼・軍士・盾兵 |
| **暴動村鎮**（原 暴動街區） | 別當第三方的靶：領主駐軍 vs 民變 | 軍士・盾兵・弓手／農兵×3・包抄 |
| **地窖寶庫**（原 金庫） | 窄室裡的破甲 | 徒步兵×2・包抄・**弩手**・軍士 |
| **哨塔**（原 前哨站） | 兩道哨卡各有繞道 | 包抄・農兵・軍士・盾兵 |
| **雙線迴廊**（原 雙線哨廊） | 換道：中道被長 reach 封住 | **槍兵**・弓手・軍士・包抄 |

- **`LEGACY_ENEMY_ALIAS` 已刪除**——`legacyEnemyUsage()` 回 `{}`，對照表完成任務。
  敵種只出現在程式定義的地圖裡、不存在於使用者存檔，所以刪掉不會弄壞任何人的進度。
  **`LEGACY_ART_ALIAS` 必須留著**：術會被寫進 roster/倉庫的 localStorage，舊存檔會帶著舊 id 回來。
- 全部 12 張地圖都標了 `medieval: true` → 那條守門測試現在對整個專案生效。
- **序章擱置**：`initRun()` 直接進路線圖。原本的第 0 關是原版的間諜題材（管制室/同僚/控制台），
  中世紀化要整段重寫敘事＝Phase 4 的事；在那之前每局都播一段不合題材的教學只會擋路。
  `startIntro`/`skipIntro`/`intro` 地圖都保留，Phase 4 重寫開場時直接接回來。
- **終局圖擱置**：`finale` 的 `roles` 清空＝不進任何抽選池（boss 池還有關隘與哨塔）。

**實測：整條戰役跑得完**
```
initRun → 路線圖(3 個起點) → COMBAT×5 → REST → BOSS → RUNWIN
六張圖全部開得起來，各跑 300 幀無例外
```

> 待觀察：**地窖寶庫**在騎士完全不動的 300 幀後我方士氣掉到 25（其他圖都還有 98–100）。
> 弩手＋軍士在窄室裡確實該比較兇，但這個坡度偏陡，實際玩過再決定要不要調。

### Phase 2.6 — 出身選擇 + 戰鬥 HUD 重排（完成，使用者提案）

**出身（ORIGINS）——取代被擱置的敘事序章**

使用者要的不是教學，是「決定第一個角色（＝你操作的騎士）有怎樣的初始裝備」。
所以這一頁只做一件事：把四套配裝攤開來比，選的是**打法**不是數值高低。
刻意讓每一套都有一個明確的弱點，**沒有全能選項**（測試會驗「能格擋的出身不能是全部」）：

| 出身 | 槽1 | 槽2 | 卷軸／習得 | 甲 | 弱點 |
|---|---|---|---|---|---|
| 守備 | 長劍＋圓盾 | 投槍＋圓盾 | 藥劑／癒手 | 鎖子甲 | 輸出最低 |
| 陷陣 | 雙手斧 | 長劍＋小圓盾 | 火油罐／包紮 | 胸甲 | **不能格擋** |
| 遊獵 | 長弓 | 短刀＋小圓盾 | 煙罐／癒手 | 厚襖 | 貼身就完蛋 |
| 習術 | 長劍＋小圓盾 | 十字弓 | 火矢／**火矢** | 厚襖 | 甲最薄 |

只覆寫 `roster[KNIGHT_ID]`，扈從仍是隨機招募。`initRun()` → `enterOrigin()` → 選完進路線圖。

**戰鬥 HUD：隊伍狀態卡改為靠左直排**

- 騎士一張完整（武器/彈藥/術的 CD），扈從各一張精簡的（名字/血/體幹細條/狀態）。
  **扈從卡不放武器與彈藥**——那些玩家既不能操作也不需要即時知道，放上去只會擠掉騎士自己的資訊。
- 挨打＝整張卡紅框＋震動（`cardHitT` 0.45s，比單位本體的閃白 0.12s 久，掃一眼才不會漏看；
  震幅隨計時器衰減）。
- 士氣狀態直接寫在卡上：動搖＝黃字、**潰逃＝閃紅字**（那代表這個人已經不聽指令了）。

> 測試踩到的坑（同一類第三次）：新加的 HUD 測試跑了 0.55 秒的 `update()`，期間敵人互毆
> 把陣營士氣打低，導致**後面**的盾兵測試裡那名盾兵處於動搖/潰逃狀態、量到的不是盾而是逃跑。
> → `__mkShield` 改成每次先 `resetFactionMorale()`。**共用世界狀態的測試必須自己負責歸位。**

### Phase 2.7 — 移除被動／戰果升階（完成，使用者定案）

> 「每一關打完的戰果升階那段全部拿掉，裡面殘留太多與這版本無關的內容了。」

問題不在機制而在**內容**：`PASSIVES` 三棵樹的節點幾乎全是槍械時代的——
發散(bloom)、換彈時間、連射 bloom 上限、開火警覺半徑、壓制。這一版近接為主、
彈藥只剩弓弩，那些節點大半沒有意義，留著只是讓每關結束多一個沒得選的畫面。

**整套拔掉**（不是只藏起畫面）：
- `PASSIVES` / `PASSIVE_TREES` / `CONFIG.passive` / 全部 `passive*` helper（約 112 行）
- `SETTLE` 場景與 `enterSettle`/`settlePromote`/`settleSkip`/`settleAdvance`/`drawSettle*`/`settleClick`
- roster 的 `tier` / `passives` 欄位、`TIER_COLOR`、名字後面的 ★
- 單位上只被寫入從不被讀取的死欄位：`passBuffT/passBuffMove/passBuffFire/stillT/momentumT/regenT/suppressedT`
- 過關 → `endMission` 直接 `advanceNode()` 回路線圖

**成長改由已經在運作的三個接口承擔**：四槽位配裝（武器/盾/甲/術）、導師節點的習得、聖物 Hooks。
「拜師」事件原本給一條被動，現在**教一個習得的術**進倉庫——正好接上術系統，也是導師節點之外的另一條管道。
測試會斷言這些名字全都 `undefined`，避免它們哪天又長回來。

**實測**：四種出身各跑一整條路線圖都到 RUNWIN，途中經過導師／事件／軍械庫／休息，沒有結算頁。

> **同一類測試污染，這次是第四次**：新加的 `endMission` 測試把 `scene` 推離 `COMBAT`，
> 而 `update()` 在非戰鬥場景是空轉的——結果**後面**所有「跑 N 幀」的測試都靜靜地什麼都沒發生
> （盾永遠不放下、快慢節奏打出完全相同的傷害）。三條測試同時變紅才發現。
> 教訓已經很清楚了：**任何動到 `scene` / `enemies` / `roster` / 陣營士氣的測試，都要自己收乾淨。**

### Phase 2.8 — 盔甲的第二層減傷（完成，使用者定案）

> 「有盔甲的 TTK 整體應該變長……可能還是要有少量被動的減傷，即使傷害沒有被盔甲保護擋下。」

**實測確認了問題不在倍率而在變異數**：改動前長劍打披甲士其實已經是 2.3 倍 TTK，
但體感是「砍四刀什麼都沒發生，然後他三刀就死」——因為 55% 完全彈開、45% **全額**，中間沒有東西。
而戰鎚把甲整個抹平（1.0 倍），甲只有「無敵或無關」兩種狀態。

```
實際減傷 = u.armor × flatResist × (1 − pierce × pierceBypass)
flatResist 0.45 · pierceBypass 0.7 · resistCap 0.75
```
**穿甲部分繞過**（使用者定案）：鎚仍然明顯最好，但板甲對任何武器都還是有意義。繞背一律無視（與彈開同規則）。

| 武器 | 徒步兵 | 披甲士 | 倍率 | 彈開 | 穿過的減傷 |
|---|---|---|---|---|---|
| 長劍 (0.35) | 3.0 刀 | 8.8 刀 | 2.3→**2.9×** | 55% | 29% |
| 雙手斧 (0.30) | 2.0 刀 | 7.7 刀 | **3.9×** | 60% | 30% |
| 戰鎚 (0.95) | 2.0 刀 | 3.1 刀 | 1.0→**1.6×** | 4% | 13% |
| 長弓 (0.15) | 3.0 刀 | 18.5 刀 | **6.2×** | 72% | 34% |

騎士也吃同一套（敵我同規則）：直接餵傷害時布衣撐 4.0 刀 → 板甲 9.7 刀。
**盾格與甲會疊加**（擋下來了、而且身上還穿著甲），既有測試的期望值已更新。

**⚠ 騎士側的實戰平衡仍未驗證，原因值得記下來**

想量「板甲騎士在真的圍毆中撐多久」時連續量到垃圾，追下去是**兩個都出在量測本身**：

1. **探針跨區段累積狀態**：同一個 VM 裡先跑了幾百次 TTK 試驗（＝殺掉幾百個敵人），
   士氣、氣勢、戰力快照全都留著；到後面的圍毆測試時敵人一開場就是 shaken，
   量到的是「他們在逃跑」而不是「甲有沒有用」。→ 改成**每組全新 VM**。
2. **修好之後仍然雙峰**：布衣不是 100/100 就是 24/100——那些滿血的場次是敵人根本沒接觸到騎士。
   **試作場的近戰圖不是可用的平衡測試台**：一場仗會不會真的發生本身就是擲骰。

所以這一節的數字只有「直接餵傷害」那一軸是可信的。實戰平衡要等**遇戰層**（下一節）——
一個保證接戰的小規模遭遇，本身就是這個專案缺的測試台。

### Phase 2.9 — 扈從裝備鎖死 + 全隊靜默（完成，使用者定案）

**扈從裝備由兵種決定、不可自由編輯**（使用者傾向：「只能使用他們設定的裝備和技能」）

理由比「簡化」更強：既然已定「扈從＝敵人、同一副身體」，若裝備能自由編輯，招到誰就不再重要
（撿到什麼都能重配）。鎖死裝備才讓「這次招到弓手還是槍兵」是真的結果。加上扈從永久死亡，
投資裝備只會製造損失趨避。
- `CLASS_LOADOUT`：各兵種對到同名敵種的武器（弓手帶弓、槍兵帶槍、農兵帶棍）＝敵我拿一樣的傢伙。
- 配裝卡只有騎士可編輯（`editable = manage && mi === KNIGHT_ID`），扈從卡標「🔒 兵種固定」。
- 第二道防線：`canEditLoadout(mi)` 守住所有 equip/unequip/allocAmmo/cycleArchetype——就算有別的路徑呼叫也擋住。
- 代價：戰利品全歸騎士。這划算——與其留一個不該用的畫面，不如砍掉。

**全隊靜默／曝光——取代舊的逐人 E/Q stance**

舊系統有兩個毛病：逐單位開關（跟指令輪盤是兩套平行的指揮系統）、且開打後對「剛走過來還沒發現你」的
敵人仍不出手。新模型把「維持靜默的條件」講清楚：**還沒有人知道我們在這**。
- 靜默中（跟隨/守住）：扈從只反擊**已交戰**的敵人，不主動破壞潛行。
- **見血或被發現→自動曝光**，之後全力交戰，不需要再下一次令。全隊一個開關（`squadRevealed`），不是逐人。
- 「集中攻擊」指令永遠無視靜默（你明確指了目標）。

★ 關鍵區分：**「敵人 ENGAGED」不等於「敵人在打我方」**。第三方互毆時敵人也是 ENGAGED，
但那不該解除靜默（你要能站旁邊看他們拼）。所以只有兩個觸發：敵人**視覺識別到我方**（不含
`aiFoe` 那條）、以及**任何我方單位造成或承受傷害**。移除了逐人 `stance`、`setStance`、E/Q 鍵。

> 測試踩到的坑（這一批就三個，都在測試自己身上）：
> ① 一條 ally-loadout 測試把診斷字串存進 `globalThis.__lock`——那正是驗證輔助函式的名字，
>    把它從函式覆寫成字串，後面所有 `__lock(true)` 全爆。**診斷全域要用夠獨特的名字。**
> ② 第三方測試原本跑 4 秒 sim，敵人偶爾走到藏在角落的玩家旁把它弄成擲骰。
> ③ 想改成把玩家設 EXTRACTED 隔離，卻觸發「全員撤離＝過關」，`update()` 直接清場。
>    最後改成**完全不 tick**——`revealSquad` 在 `damageUnit` 裡是同步的，一 tick 反而引入別的系統。

### Phase 3.0 — 遭遇層（完成，使用者定案）

> 「每一場戰鬥都應該是真的小規模衝突……現在因為是舊關卡平行移植所以敵人陣容分散且人數很多。」

診斷：問題不在數量，在**設計單位**。敵人配置烤死在地圖資料（`enemySpawns`），所以每張圖永遠是
同一批人散佈在同一個大場地。`ENCOUNTERS` 把**誰／幾個／開場什麼狀態**抽成獨立層，地圖只提供地形與
**生成錨點**（`encounterAnchors`，預設借用地圖作者擺好的 spawn 節點）。遭遇聚在錨點附近＝自然小規模。

`opening` 直接對應現有狀態機，**零新 AI**：

| 遭遇 | opening | 對應狀態 |
|---|---|---|
| 攔路賊・獵犬隊・戰團・前鋒隊 | `ambush` | 開場 ENGAGED、直接壓上 |
| 巡邏隊・斥候組 | `patrol` | IDLE 巡邏中，可迴避或伏擊 |
| 哨站 | `sentry` | IDLE 靜態守點，被發現才動 |
| 混戰 | `threeway` | 兩個敵對陣營都 ENGAGED 打對方，你是第三方 |

- `map.encounters: true` 才走遭遇層；**王節點永遠用烤死的 spawn**（boss 要可控）、riot 保留手刻第三方。
- 遭遇與士氣相乘：四人巡邏隊死一個就崩＝完整的 15 秒弧線。
- **移除 vault（雙門地窖），新增 field（曠野）**：超開放、只有零星石柱與草叢，一切靠陣型與站位。

**遭遇層＝這個專案一直缺的實戰測試台**

Phase 2.8 量不出盔甲的實戰平衡，因為試作場「一場仗會不會發生本身就是擲骰」。遭遇層保證接戰，
於是同一組呆板自動 AI 就跑出了兩件事：

*難度曲線*（騎士穿鎖子甲、自動 AI、各遭遇 8 場）：
```
攔路賊 8/8 剩100 · 巡邏隊 8/8 剩64 · 獵犬隊 8/8 剩100
哨站 5/8 剩52 · 戰團 8/8 剩56 · 前鋒隊(T3雙菁英) 1/8 剩48
```
試作場做不出這種 8/8→5/8→1/8 的漸層。前鋒隊 1/8 很硬，但那是**呆板 AI 的下限**（不格擋不閃不利用士氣），
且只出現在後段——先觀察，不急著調。

*盔甲的實戰平衡*（固定打「戰團」、各 12 場）：
```
布衣 剩50 · 厚襖 剩54 · 鎖子甲 剩71 · 胸甲 剩75 · 板甲 剩77
```
單調遞增、跨度合理，代價是移速 −18%。**Phase 2.8 留下的坑補上了。**

### Phase 3.1 — 開局單騎 / 事件加權 / 追兵軟取消 / 節奏 ×1.2（完成，使用者定案）

四項插隊調整（使用者要在此之後討論事件池）：

1. **開局只有騎士一個人**。`initRun` 的 `roster` 從三名扈從改成單一騎士（origin 覆寫）。
   敗騎孤身上路，人手在旅途中招募——這也是下一項（事件加權）的動機。
2. **事件加權提高**：`typeWeights` EVENT 2→4（與 COMBAT 4 齊平）。事件是招募人手的主要管道，
   單騎開局後更需要它。實測 8 次生成，事件節點佔比 >15%。
3. **追兵軟取消**：`CONFIG.reinforce.enabled = false`——敵人沒理由特別追一個落敗騎士。
   計時上限拉到 1e9（追兵觸發條件幾乎永不成立）、UI 計時條隱藏。機制與數值都留著，改回 `true` 即復原。
4. **整體節奏 ×1.2**：單一旋鈕 `CONFIG.tempo = 1.2`。時間類（前後搖、預告瞄準）× tempo＝更長；
   速度類（移動）÷ tempo＝更慢。套用點只有四個：`spdOf`（我方移動）、enemy 移動三處、
   `startSwing`（揮擊三段）、`aimTimeOf`（預告）。**刻意不動士氣/CD/耐力回復等純邏輯計時**。設回 1.0＝原速。

**孤騎可玩性實測（守備出身，純進攻自動 AI＝下限）**
```
攔路賊/獵犬隊/戰團(ambush) 12/12 全勝 · 混戰 12/12 滿血（兩夥自己拼掉）
帶弓的巡邏/斥候 0–4/12 —— 但那是純近戰駕駛追不到放風箏的弓手，滿血率高＝弓手沒傷到
```
ambush 型孤騎打得贏；帶遠程的遭遇追不到，正是「該招人手」的論據（真人會貼上去或用盾/遠程）。

> 修 flaky 時抓到一個真 bug：曝光判斷原本放在 `Hooks.fire('hurt')` 之後，但**板甲騎士被彈開的
> 那一擊會在 `armorBlocked return` 提早返回、跳過曝光**。被攻擊（即使被彈開/擋下）就是已被發現，
> 所以把曝光移到 `damageUnit` 最前面。這條測試每五次飄一次才逼出這個洞。

### Phase 3.2 — 休息節點：隱藏內容 + 固定深度必經（完成，使用者定案）

Phase 4 前的最後一項插隊調整。

**導師歸類成休息節點，內容進入才揭曉。** 路線圖上所有「休整地」畫成同一種節點（`type: 'REST'`），
底下藏一個 `site`：目前 `camp`（整補）與 `tutor`（導師），未來鐵匠／商人／情報販子／營地事件都掛在這裡。
點進去才知道是哪一種——**除非有斥候隨從或（尚未設計的）偵查被動**先探明（`hasScoutRecon()` 現回 false，
是那個未來機制的單一接縫）。已去過的節點自然揭曉。
- `TUTOR` 不再是獨立的節點類型；`selectNode` 對 REST 節點呼叫 `enterSite(siteOf(node))` 分派。
- 顯示：未知＝`⌂ 休整地`；已知＝該 site 的字形與名稱。

**不管走哪條路，都在相同深度必經休息。** genRoadmap 每隔 `restEvery`（=2）欄設一道**休息牆**＝整欄 REST。
因為每條路徑在每一欄恰有一個節點，整欄 REST 就保證每條路都在該深度踩到休息（未必收束到同一格，
而是同一進展深度）。實測布局：`戰 → 隨機 → 《休》 → 隨機 → 《休》 → 隨機 → 王`。

> 這改動也把 `typeWeights` 的 `TUTOR` 併回 `REST`（1.2+1.0 → 2.0），再由 `siteWeights` 決定
> 休息節點裡實際是什麼（camp 3 : tutor 1.5）。加新休息類型＝往 `REST_SITES` + `siteWeights` + `enterSite` 各加一條。

### Phase 3.3 — 新序章（完成，使用者定案）

用既有的 cutscene beat 序列器（聚光 `reveal`／點擊前進 `say`／腳本攻擊 `shoot`／`gate`）重寫開場：

1. 敗騎自**屍山血海**中醒來（`corpses` 叉叉 + `pushDecal` 血跡當 placeholder），意識到己方已敗。
2. 眼前一隻狼背對玩家啃食屍體（`wolf`，facing 朝外＝背對）；腳本引導繞到牠身後 → 按左鍵**一擊處決**。
3. 接管操作往前走 → 接近正在搜刮的敵兵 → **暫停、跳出文字卡做操作說明、點擊關閉**（← 唯一的新機制）。
4. 文字卡關閉後敵兵開打 → 擊敗 → 前往撤離點 → 進路線圖。

**新增的只有一個 beat kind：`card`**（畫面正中的 modal 說明卡，壓暗背景、點擊 ack 關閉）。其餘全是重用：
- `shoot` beat 改成 melee-aware（近戰揮刀視覺／遠程槍口火光），並可指定 `victim`。
- 敵兵的 `introFrozen` 旗標（enemyTick 早退）＝文字卡結束前不行動不偵測；配合牠面向下方（搜刮姿態、
  背對動線）自然不會提早發現你。
- 序章戰場改用 `field`（曠野）＝開闊，適合屍山。`chooseOrigin` → `startIntro`（`CONFIG.introEnabled`，
  可關）→ 撤離 → `finishIntro` 進路線圖（不再走舊的「序章休息」）。Esc→`skipIntro` 直接進路線圖。

**headless 逐拍追蹤（可跑完到撤離）**
```
對話「……我還活著？」→ 繞到狼背後 → 一擊處決狼 → 往前走 → ★文字卡(操作說明)
→ 敵兵開打 → 擊敗 → 撤離 → scene=MAP
```
> ⚠ headless 只能驗「每一拍都跑得動、到得了撤離、不炸」。**視覺體感（聚光構圖、狼啃屍的可讀性、
> 屍體密度、文字卡排版、節奏）要玩家親眼看過再調**。目前是能跑的第一版。
> 另外：序章目前**每個 run 都播**（Esc 可跳）。要不要改成只播第一次，是待你決定的設計選擇。

### 事件池（設計草案，尚未實作 — 使用者定案的優先項）

**地基已確認齊備**（`runFlags` 全域旗標 + 現有事件鉤子，見下）。設計準則沿用原版：
每事件都有**安全選項**、風險**知情**、下檔**有上限不致命**；並盡量咬合這一版新做的系統（士氣/氣勢、遭遇層、術/卷軸、招募）。

**全域旗標地基（run 內，`initRun` 重置；不進 localStorage）**
- `runFlags{}`＝跨節點記憶：`setFlag(k,v)` / `flag(k)`。事件的 `cond:f=>…`（旗標當入口）、`onEnter:f=>{…}`（進入時擲隱藏身分，吃 srand＝可重現）、選項的 `set:{…}`（記住決定）都吃它。
- `pendingEvents[]`＝**延遲後果**佇列：選項 `queue:['id']` 塞入 → 下一個事件節點強制觸發（背叛→伏擊、施恩→報恩都靠這條）。
- 結果詞彙：`recruit / combat{map} / gamble{p,win,lose} / trade{cost,gain} / loot{n} / heal / damage / maxhp / item / equip / off / armor / relic(傳授術) / rest / nothing`，且可組合。

**實作前要先蓋的共用新機制（3 個）**
- **N1 · 下一場戰鬥修正**：一個 `nextBattleMod` 旗標，在 `startMission` 讀取後套用一次即清空（食物 buff/debuff、禱告的開場氣勢都靠它）。**目前沒有這個接縫**——所有既有結果都是即時的。
- **N2 · 事件戰鬥打贏後給獎**：`combat` 結果目前打完只是 `advanceNode`，沒有戰後獎勵。要「打贏→拿獎」得在 event-combat 的 `endMission` 收尾補一個「發放預存獎勵」的鉤子（比武騎士、協助農民若走真打都需要）。
- **N3 · 有稀有度下限的獎勵**：`loot`/`equip` 現在吃全域稀有度權重；「較好的裝備」需要一個 `minRarity` 參數（或直接指定 id）。小改。

> 若某事件不想動 N2，可退回用 `gamble`（技藝檢定，不真的打）→ 直接發獎＝純換皮零新碼。逐事件可選。

**使用者提的四個（換皮對照）**

| 事件 | 選項 → 結果 | 咬合的機制 | 純換皮？ |
|---|---|---|---|
| **找到食物** | 吃→`gamble`（可能腐壞）→ 下場 buff／debuff · 不吃→`nothing` | **N1**（下場修正） | 需 N1；退而求其次可用即時 `heal`/`damage` |
| **農民被劫掠** | 協助→ 拿 `item`/`equip` · 不理→`nothing`；協助失利→`damage` | `gamble→loot`（換皮 `armory`）或真打（**N2**） | 走 gamble＝純換皮 |
| **認出逃亡士兵的紋章** | 「我們同侍一主，合作吧」→ **加入/攻擊/逃跑** 三擇一 | `onEnter`+srand 擲 disposition；分支 `recruit` / `combat` / `nothing`（換皮 `boy` 場景事件） | 純換皮（3-way 擴充 boy 的 2-way） |
| **比武認識的敵騎士** | 單挑 → 打贏他認可你 → 較好裝備 | `combat`（單騎兵遭遇）+ **N2** + **N3**；或 `gamble`（武藝）→ 好 `equip` | 走 gamble＝純換皮；要真單挑＝N2+N3 |

**我補充的同類簡單事件**（都能用現有詞彙＋最多沾 N1）
- **廢棄神龕・禱告**：禱告 → 下場**開場氣勢**（`grantMomentum` 咬合士氣系統，走 N1）／不擾亡者→`nothing`。
- **渡口收費**：船夫要買路（`trade` 付 gear/item）安然渡河／硬涉水→`gamble`（−HP）。純換皮。
- **潰兵營火**：收編（`recruit`，但戒心重→`gamble` 加入/逃）／打劫（`combat`→`loot`）。咬合招募。
- **戰場拾荒**：搜刮屍陣→`gamble`→ `item`／驚動食腐獸的 `combat`。呼應序章的屍山題材。
- **重傷的同袍**：治療（`trade` 付一張卷軸/消耗品 → `heal` 該員）／拋下（該員離隊或低血續戰）。咬合永久死亡的張力。
- **迷途難民**：護送 → `queue` 一個「報恩」事件到後段（延遲後果的正面版，純換皮 boy→ambush 的 queue 模式）。

**現有 EVENTS 的換皮／退場對照**（原版殘留的間諜題材要清）
- `boy`（sleeper/父親）→ **逃亡士兵**（換皮，3-way）；`wounded`（探員）→ **傷兵/難民**（中世紀化）；
- `armory`/`riot`/`ambush`→ 保留、微調文本即可；
- `broker`（黑市掮客）/`veteran`（老兵指點）→ **移到休整地的 site**（商人/導師），不再當隨機事件。

### 事件池 · 複雜事件（設計草案，尚未實作）

使用者提的三個。**關鍵發現：事件本身很薄，真正的工作是它們共用的兩個「目前不存在」的大系統**——
`grantMomentum` 只是單場、會衰退的；這三個事件的「氣勢」全是**跨戰鬥**的（團聚給暫時氣勢、
女巫詛咒持續到人走光、石中劍裝著就一直加）。裝備「強化」則整個系統都還沒有（tier/被動已刪）。

**先蓋的大型共用系統（比事件本身大得多）**
- **S1 · 跨戰鬥氣勢修正層**（`runMomentumMods`）：一串 active 修正 `{source, amount, lifetime}`，
  每場 `startMission` 加總後餵給 `grantMomentum('player', …)`。lifetime 三型別涵蓋全部需求：
  `oneBattle`（團聚的暫時氣勢）／`untilGone`（女巫詛咒＝綁一組見證者，人走光才解）／`whileEquipped`（石中劍）。
  這也把先前的 **N1（下一場修正）** 一起吃掉＝N1 就是 lifetime:oneBattle 的特例。
- **S2 · 裝備強化系統**（**全新，從零設計**）：事件 2 產生一次強化、事件 3 說「不能再強化」——兩者都預設它存在。
  要先回答「強化是什麼」：+傷害階級？附魔槽？詞綴？這是 Phase 4 級的獨立系統，不是小鉤子。**先決定形狀再談這兩個事件。**
- **S3 · 護送乘客 + 節點註記 + 錯過偵測**（事件 1 專用）：
  非戰鬥「乘客」狀態（`runEscort`，不進 roster、不參戰）；在路線圖某節點打**註記**（節點掛一個 payoff）；
  抵達＝團聚、**推進超過該節點的深度卻沒去**＝錯過。錯過偵測靠「已完成節點的最大 col ≥ 註記 col 但註記未完成」。
- **S4 · meta 貨幣改名 情報→榮譽**（`meta.intel` ~30 處，機械式改名/別名）＋ **meta-gate 事件**
  （`cond` 是 JS 函式，本來就能讀 `meta`＝零新碼）。

**三個事件的拆解**

1. **走散的隨軍男孩**（S1 + S3）
   - 遇見 → 帶上他＝設 `runEscort=boy`、在路線圖挑一個未來節點打註記。
   - 抵達註記點＝團聚 → `loot`（物資）＋ S1 `oneBattle` 氣勢+。
   - 錯過（從別條路越過該深度）＝男孩難過離開 → S1 `oneBattle` 氣勢−（一次性打擊）。
   - 待決：註記點怎麼選（隨機一個 2~3 欄後的節點？）、UI 怎麼在路線圖標出來、錯過的當下在哪裡告知玩家。

2. **女巫的獻祭**（S1 + S2）
   - 交出一名手下（**移除一名 roster** — 新的小結果 `sacrifice`）→ S2 強化一件裝備。
   - 代價：綁「當下全隊」為見證者，S1 `untilGone` 氣勢− 持續到見證者全部離開/死亡；
     且每場有機率一名見證者**直接離隊**（新的 desertion 擲骰）。
   - 待決：S2 得先存在；「強化」的幅度 vs 詛咒的重量怎麼平衡；desertion 機率。

3. **湖中石中劍**（S1 + S4，且 S2 只用到「不可強化」旗標）
   - meta（榮譽）≥ 門檻才能拔（`cond: () => meta.intel >= X`）。
   - 一把特殊武器：傷害同長劍、**pierce 1.0（完全穿甲）**、標記 `noEnhance`。
   - 裝備著時 S1 `whileEquipped` 氣勢**大幅**+。
   - 這個最獨立：不依賴 S2 的實作（只用一個旗標），主要靠 S1 + S4 + 一把新武器。**可先做，當 S1/S4 的試金石。**

**建議的實作順序**（若之後要做）：S4 改名 → S1 氣勢修正層 →（事件 3 石中劍當 S1 的驗證）→ S3 護送（事件 1）→ 最後才是 S2 強化系統（事件 2，最大）。

**進度（已實作）**
- ✅ **S4 · meta 貨幣改名 情報→榮譽**：`meta.intel`→`meta.honor`（`normalizeMeta` 有舊存檔遷移）、
  `CONFIG.meta.honorPerX`、`unlockWithHonor`、HQ/主選單/結算顯示全改。**AI 的「共享情報」（視野共享）不動**。
- ✅ **S1 · 跨戰鬥氣勢修正層**：`runMomentumMods` + `addRunMomentumMod` + `applyRunMomentum`（`startMission` 開戰時套一發）。
  三型 lifetime（oneBattle／untilGone〔綁見證者〕／whileEquipped〔綁武器〕）全部有測試。
  事件接縫：新增 `momentum` 結果型（`{amount, lifetime, witnesses?, weaponId?, source?}`）。
- ⏳ S3 護送、S2 強化＝未做。

### 休整地的「人」+ 裝備強化系統（設計草案，使用者定案：一起設計，做完再回頭弄事件）

使用者定案把**強化系統長在休整地的鐵匠 site 裡**——這是對的：鐵匠本來就是強化的所在。所以這一塊要一起設計。

**休整地 site 目前狀態**：`REST_SITES` 已有 `camp`（整補）、`tutor`（導師）；框架（進入才揭曉、斥候可探明、
`enterSite` 分派）都在。要補的人：
- **鐵匠 blacksmith** — **裝備強化**（S2 的家）＋修復。
- **商人 merchant** — 買賣（收現有的 `broker` 事件進來）。
- **情報販子 info** — 花榮譽？或用某資源探明前方路線圖節點（呼應斥候探明機制）。
- **營地事件 campevent** — 隨機小事件（把部分簡單事件挪進休整地）。

**S2 · 裝備強化「是什麼」——待決的核心問題**（決定形狀才能動）
候選方向（先討論，不預設）：
1. **階級制 +N**：武器/甲 +1/+2/…，每階小幅加傷害/減傷。簡單、好懂，但容易變成純數值膨脹。
2. **附魔/詞綴**：掛一條效果（火焰＝額外體幹傷、鋒銳＝+穿甲、堅固＝+格擋…），咬合現有的傷害/體幹/穿甲/格擋維度。
   比階級有趣、可與 Hooks（聖物那套）共用接縫，但要設計詞綴池。
3. **混合**：少量階級 + 一個詞綴槽。
- 石中劍的「不能再強化」＝一個 `noEnhance` 旗標，等 S2 定案才有意義。
- 女巫事件「交出一人換一次強化」＝一次 S2 操作 + S1 的 untilGone 詛咒（兩者都已可掛）。

**S2 · 裝備強化系統 — 定案（使用者拍板）**

> 「精良程度不是直接體質，而是能強化幾次。」

- **精良程度＝強化 charge 預算**（能打幾次），本身不加數值。**從稀有度衍生**（common=1 … legendary=5）
  ＝掉到高稀有度的武器就是那個精良度。精良1 打一次就滿；精良3 上限更高。
- 每次強化＝消耗 charge，內容分兩種、**由不同的人賦予**：
  - **鐵匠**：鋒利（+傷害）／平衡（−耐力消耗），**連哪個都隨機、數值也隨機**，1 格。
  - **導師**：詞條（affix），1~3 格（高級詞條吃多格）。**導師因此多一個功能**（除了教術）。
  - **女巫**：專屬詞條（走同一條 affix 管線，只是池不同、`source:'witch'`）。
- **詞條＝掛在武器上的迷你聖物**：純數值的（破甲＝+pierce）直接併進 `effWeapon`；有效果的（火焰）訂閱 Hooks，跟聖物同機制。
- **石中劍＝一把預先打滿的實例**（charges 用完＝「不能再強化」自然成立）：滿鋒利＋滿平衡＋一條 rally 詞條。
  rally 詞條 → 接 S1 的 `whileEquipped` 氣勢。**所以石中劍與女巫都不是特例，是同一套系統的資料點。**
- 先只做武器；甲/副手之後同一模型接。

**資料模型**：武器實例 `{ id, quality, enh: [{kind, charges, value?/affix?}] }`；`effWeapon(inst)` 併出戰鬥用的 `u.weapon`。

**分階段實作**（避免半成品）：
1. ✅ **純引擎**（已做）：`weaponInst`／`qualityOf`（稀有度→格數）／`effWeapon`（併鋒利/平衡/純數值詞條）／
   `enhanceWeapon`（鐵匠，隨機鋒利/平衡+隨機值）／`applyAffix`（導師/女巫，`source` 限定）／charge 預算／
   `AFFIXES` 表（破甲/鋒棱/烈焰/戰意/巫紋）／`stoneSwordInst`（預打滿）。11 項測試。
   順帶：隊員加 **`uid` 穩定身分**（女巫詛咒的見證者必須靠 uid——名字會被離隊者的後繼重用，這是真 bug）。
2. **實例化倉庫/roster**：weapon 存法從 `{id}` → 帶 `{id,quality,enh}`；`makeWeaponSlot` 用 `effWeapon`；掉落/拾取帶著走。
3. **鐵匠 site + 導師加功能 + 配裝卡顯示精良度/詞條**：UI 層。
4. **接事件**：女巫（獻祭→強化＋S1 詛咒）、石中劍（榮譽 gate→給預打滿實例）。



### Phase 4 — 題材 / 敘事 / meta 重塑
- [ ] 路線圖節點與場景敘事改寫（焚村/林道/渡口/關隘；野營/鍛冶/路上遭遇）。
- [ ] 主選單 / 總部(HQ) / 結局改寫為歸鄉・贖罪主題（移除原間諜「沉睡者」敘事、The Patriot 致敬等）。
- [ ] 以編輯器手刻中世紀地圖；source/sink 經濟平衡。
- **驗收**：一條可玩的歸途路線，具開場—中段—終局敘事。

### Phase 5 — 打磨與（選配）架構
- [ ] 可讀性 / juice / 平衡調校（沿用 `TUNE` 面板）。
- [ ] **`CONFIG.juice` 的 `camKick` / `shakeFire` / `shakeHit` / `shakeKill` 目前全是 0**——原版把整條螢幕震動管線接好了卻沒開。Phase 1 近戰打擊感可直接從這裡拿甜頭。
- [ ] （選配）拆分 ES 模組以利維護（純 vanilla，不引入引擎）。
- [ ] （選配）發佈：Hugo 上架、於 `content/projects.md` 連結。

### Phase 6（候補，未排程）— 其他模式
- 原版的 `extraction_arcade/`（搜打撤 arcade 變體：寶物/滿意值撤退/獵人 FFA/本機排行榜）可作為**比武大會／生存者模式**的土台。目前未複製進本 repo（會與 Homeward 的 `index.html` 大幅分歧），需要時自 `darkbearlab/sleeper_roguelite` 取回。

---

## 開發守則（承襲原版設計憲章，因應「去附身」調整）

- **攝影機完全踏襲原版**，任何簡化不得退化瞄準/旋轉手感。
- **友軍永久死亡（down = dead）**：壓力前移到「預防與護送」。
- **追兵軟計時只是壓力調節，不是失敗畫面**；但**騎士死亡 = run 結束**是唯一硬敗北。
- **敵人不做空間尋路**，沿用手刻 waypoint 圖（reservation + hysteresis 必留）。
- **數值先求手感**，集中於檔頂 `CONFIG` / `TUNE`，先跑得動再調平衡。
- **陣營資料驅動**：新增勢力＝加一條 `FACTIONS` + spawn 指定 id，戰鬥/AI/繪製不改碼。
