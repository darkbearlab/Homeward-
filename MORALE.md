# 士氣系統重設計（品質質量模型）— 設計文件

> 用「**品質質量**」取代現在那個抽象的 0–100 士氣 + 另一張靜態戰力快照。
> 一方的士氣＝場上各單位品質（吃 HP）的加總；一進交戰就逐幀比兩邊的質量差、判潰散；
> 傷害與特定條件疊一層會衰退的震盪。**孤騎問題被結構性解掉**（騎士是一份「有限」的高品質，不再是幽靈般的 100）。
> **狀態：設計定案、待實作。**

## 使用者定案
1. **品質吃 HP**：被打殘的單位貢獻的士氣就變少（傷害＝士氣壓力，很平滑）。
2. **騎士結構品質 = 2× 徒步兵**；另外約 **1× 由「氣勢錨點」補**，且**開戰後快速衰退**——＝大人物在場給的心理安穩感，一旦真打起來就消失。
3. **恢復暫時沿用現況**（regenDelay + baseRegen + 騎士光環）。

## 模型

### 1) 單位品質
```
quality(u) = qBase(兵種) × (qHpFloor + (1−qHpFloor) × clamp(HP/maxHP,0,1))
```
`qHpFloor = 0.4`（殘血也還有 40% 存在感）。`qBase` 表（徒步兵＝5 當基準）：

| 兵種 | qBase | | 兵種 | qBase |
|---|---|---|---|---|
| 平民 civilian | 2 | | 弩手 arbalest | 5 |
| 農兵 levy | 3 | | 披甲士 manatarms | 10 |
| 戰狼 wolf | 3 | | 軍士 serjeant（菁英） | 12 |
| 弓手 archer | 4 | | **騎士 knight** | **10（＝2×徒步兵）** |
| 徒步兵 footman | 5 | | 槍兵/盾兵 | 6 |

扈從用 classId 查同一張表。

### 2) 陣營質量
```
structMass(f) = Σ quality(u)              // 存活單位（吃 HP）
effMass(f)    = max(0, structMass(f) + momentum(f))
```
`momentum(f)`＝一個**會衰退的疊加池**（沿用現有 momentum 機制、擴義）：
- **騎士錨點**（只給我方、騎士在場）：`+anchorAmt(≈5＝1 徒步兵)`，**從交戰(ENGAGED)那刻起於 anchorDecay 秒內線性衰退到 0**＝安穩感開戰後快速消失。
- **戰鬥震盪**：首殺 / 斬將 / 招架 → 給對應方一發會衰退的加成；**單位陣亡 → 其陣營一發衰退的負震盪**（＝雪崩的來源）。

### 3) 拉鋸線＝質量占比
```
line(f) = 100 × effMass(f) / (effMass(f) + effMass(最強敵對陣營))   // 0..100，50＝質量均勢
```

### 4) 潰散判定（**觸發時機不變**：ENGAGED 才逐幀判）
```
單位潰散(shaken)  當 line(f) < breakAt(u) = 50 × BREAK_PCT[兵種]/100
```
沿用現有 `BREAK_PCT`（農兵 85＝門檻 42.5、徒步兵 55＝27.5、披甲士 20＝10）＝越硬越晚崩。
`breakHysteresis` 脫離、`breakJitter` 出生浮動、shaken→潰逃、性格切換、潰逃脫離戰場——**全部沿用**。
**雪崩**：一個崩→陣營負震盪→其他人 line 再降→序列崩（農兵先、披甲士最後）。

### 5) 恢復（沿用現況）
負震盪隨時間衰退（regenDelay 後開始）；騎士光環（knightAura / regenPerRallied）給聚攏在身邊的我方一點正震盪＝拉回有效落後。結構質量不會「長回來」（死者不復生），恢復只作用在震盪層。

## 為什麼這樣就解掉孤騎問題
- 孤騎＝我方 effMass ≈ 10（結構）+5（錨點，會衰退）＝開場 ~15、之後 ~10。一群 6 徒步兵＝30。
- `line(敵) = 100×30/(30+10) = 75`（遠高於 27.5）＝**敵人是質量領先方、穩得住**，不會被一兩刀嚇潰。
- 你砍倒他們＝敵方質量下滑；**每次擊殺又給倖存者一發負震盪**（+首殺/斬將尖峰）→ 兩者疊加把 line 壓下去 → **農兵先崩、雪崩**。要砍出實質減員（大約半數上下、隨敵群大小自動縮放）才會潰。
- 「品質加總」本來就把**人數×素質**一起算了，所以不用任何人數 hack——**上一版的 `forceWeight` 直接移除**。

## 對照現況（要動什麼）
| 現況 | 改成 |
|---|---|
| `factionMorale` 平坦 0–100 + 死亡/崩潰依 share 扣 | 士氣＝`structMass`（活單位品質、吃 HP）＝**活的、自動反映傷亡** |
| `snapshotFactionForce`（靜態戰力快照）+ 我加的 `forceWeight` 項 | **移除**；質量每幀即時算 |
| `unitWeight`（菁英3/其餘1） | `quality`（細緻 qBase 表 × HP%） |
| `moraleLine` = 50+(士氣差)/2、`resolveOf` = line×(morale/max)^absExp | `line` = 質量占比×100；`resolveOf` = line（HP 已進質量，不再需要 absExp 項） |
| momentum 池（首殺+18…） | 保留、擴義：加**騎士錨點**（衰退）+**陣亡負震盪** |
| moraleEligible(ENGAGED) / 崩潰狀態機 / 潰逃 / Hooks(death/kill/parry/hurt) / 騎士光環恢復 | **全部沿用** |

## 待玩測調校（v1 起點，非最終）
- `qBase` 表、`qHpFloor=0.4`、`anchorAmt≈5`、`anchorDecay≈6s`、陣亡負震盪量、首殺/斬將尖峰、`BREAK_PCT`。
- **調校目標**：孤騎 vs 6 徒步兵＝砍掉約半數才潰；均勢對打與現在手感相近；披甲士/菁英明顯更硬。
- 用 headless 模擬幾個標準情境（孤騎 vs 3/4/6/8、均勢、被伏擊）對數字。

## 分階段實作
1. `quality` + `massOf` + `line`（取代 `resolveOf`/`moraleLine`），移除 `forceWeight`/靜態快照。HUD 拉鋸條改讀質量占比。
2. momentum 擴義：騎士錨點（ENGAGED 起衰退）+ 陣亡負震盪；首殺/斬將尖峰改成質量當量。
3. 恢復沿用現況接上（震盪衰退 + 光環）。
4. Headless 模擬 + 調數字到目標手感。

## 待決 / 風險
- `anchorDecay` 多快算「快速消失」？（v1 抓 6s 線性；你要更快/更慢再說）
- 陣亡負震盪 + 首殺/斬將尖峰的量＝決定「砍幾刀開始雪崩」的主要旋鈕，實測調。
- `factionMorale` 現在被 HUD/事件 momentum/存檔跨關恢復等多處引用，改成質量模型時這些接縫要一起理（事件的 `momentum` 修正直接進 momentum 池即可）。
