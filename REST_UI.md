# 休息房 UI 重設計 + 戰鬥操作對齊（PoE 風）— 設計文件

> **狀態：計畫定案，待實作。** 使用者要把休息房的配裝介面改成 PoE 道具面板風，且只顯示騎士；
> 同時把戰鬥操作對齊 PoE（數字鍵＝施展自由位、滾輪＝換裝）。三項決策已拍板（見 §8）。

## 1. 現況與問題
- 休息房 `drawRest` 對**整個 roster** 都畫一張配裝卡（`drawLoadoutCard`，234×232，一排 5 張），但
  `canEditLoadout = (mi) => mi === KNIGHT_ID`——**只有騎士能改**，扈從的卡是「畫出來卻鎖住」＝浪費版面、誤導。
- 配裝互動是「選取再放置」（`restSel` = 選一個倉庫物 → 點卡上的槽位裝上）。

## 2. 使用者要的
1. **只有騎士**顯示完整配裝介面（不是全顯示然後鎖）。
2. **PoE 道具選單風**：整塊視窗放**右側**；**上半＝當前裝備、下半＝背包/倉庫**；**上下拖曳穿脫**。
3. **戰鬥操作對齊 PoE**：本來的兩個自由位改成**熱鍵直接施展**；**數字鍵＝用自由位**（不再切換裝備）；**滾輪＝切換裝備（武器組）**。

## 3. 可行性：資料層已經齊備（低風險）
配裝的**穿/脫 API 全都在、且雙向、且已鎖定騎士**——重設計基本上是「換一套 UI + 拖曳輸入」蓋在既有函式上：
- 裝上：`equipWeaponFromWarehouse(whIdx, mi, slot)` / `equipAbilityFromWarehouse(whIdx, mi)` / `equipGearFromWarehouse(whIdx, mi, kind, field)`
- 卸下：`unequipWeapon(mi, slot)` / `unequipAbility(mi)` / `unequipGear(mi, kind, field)`（都會把換下的推回倉庫、帶著強化）
- 丟棄：`discardWarehouse(whIdx)`；全部 `canEditLoadout(mi)` 守門（mi 一律傳 `KNIGHT_ID`）。
- 自由位在戰鬥中**本來就是熱鍵施展**（`castArt`；現在 3/4 鍵已是「用」，1/2 鍵才是切武器組）；換裝**本來就在滾輪**（`swapWeapon` 切 0↔1）。

→ **結論：沒有阻擋性問題。** 主要工作在「畫面重排 + 拖曳輸入」與「數字鍵改綁自由位」。

## 4. 新版佈局
```
┌──────────────────────────────┬───────────────────────┐
│ 戰報（第 N 關肅清 · 撤出/陣亡）           │  ▸ 騎士配裝（PoE 面板）  │
│                               │  ── 裝備（上半）──       │
│ 隊伍列（compact，只讀）                 │  武器組1  [主][副]     │
│  ▸ 騎士  [class/hp/主武器]              │  武器組2  [主][副]     │
│  ▸ 扈從…（名字/兵種/hp/主武器）           │  甲       [    ]      │
│    ＝也是「指定對象」的點選來源             │  自由A    [卷軸]      │
│    （升級某人／離隊／獻祭）                │  自由B    [習得]      │
│                               │  ── 背包/倉庫（下半）──   │
│ 搜刮三選一 / 行動列 / 繼續               │  [▦][▦][▦][▦]…       │
└──────────────────────────────┴───────────────────────┘
```
- **右側面板**（約 360–400px 寬、近全高）：只有騎士。上半＝結構化的裝備槽、下半＝倉庫格。
- **左/中**騰出來給：戰報、**隊伍列**（只讀，但仍是 offers 指定對象的點選來源）、搜刮三選一、行動列/繼續。
- 裝備槽是**有型別的**：武器組槽收武器（副手槽收盾或單手近接）、甲槽收甲、自由 A 收道具/卷軸、自由 B 收習得、（裝備槽收 equip）。

## 5. 拖曳互動（新輸入）
- 休息房改用 `mousedown → mousemove → mouseup`（編輯器已有拖拉先例）：
  - **mousedown** 命中倉庫格或裝備槽 → 拿起（`dragItem` = {from:'wh'|'equip', …}），游標跟一個 ghost。
  - **mousemove** → ghost 跟游標。
  - **mouseup** 落在合法目標：
    - 倉庫物 → 裝備槽（**型別相符**）＝裝上（呼叫對應 equip 函式）。
    - 裝備槽 → 倉庫區＝卸下（呼叫對應 unequip 函式）。
    - 落在非法/空白 → 取消（彈回）。
  - **型別守門**：拖到不相容的槽＝不接受（視覺提示紅框）。
- 保留一個輕量 fallback：**雙擊倉庫物＝快速裝到第一個相容空槽**（無滑鼠拖拉也能玩）。`restSel` 選取式可留可退。
- **倉庫容量**：卸下會推回倉庫，若有 `overCap` 上限要擋/提示（沿用現有 `overCap`）。

## 6. 戰鬥操作對齊（PoE）
| 操作 | 現在 | 改成 |
|---|---|---|
| 數字鍵 1,2 | 切武器組 1/2 | **廢除切裝**（改滾輪） |
| 數字鍵 3,4 | 用自由位（已是） | **數字鍵改綁自由位**：`1`＝自由A(道具/卷軸)、`2`＝自由B(習得) |
| 滾輪 | 換武器組（已是 `swapWeapon` 0↔1） | 維持＝**換裝**（兩組間切換；未來多組＝循環） |
| X | 換武器組 | 保留為替代鍵（可留） |
| 右鍵 / 空白 | 技能（習得） | 保留右鍵＝習得（自然的施法鍵）；空白可退役 |
| C | 道具 | 由數字鍵 `1` 取代；C 可保留為別名 |
- 自由位只有 **2 個** → 數字鍵用 **1,2**；**3,4 先留空**（若日後擴充自由位或做 PoE 藥水帶再啟用）。
- HUD 的自由位提示要改成新鍵位（顯示 `1`/`2`）。

## 7. 保留、不動的部分
- 戰報、搜刮三選一（`restPhase` OFFERS）、指定對象升級（TARGET）、離隊/獻祭、繼續（DONE）＝機制不變。
- **隊伍列仍要在**（只讀）：一來給玩家戰場感知（誰在隊上、兵種、血），二來是 offers「指定對象」的點選來源（否則升級/離隊沒得選人）。差別只是**不再是可編輯的配裝卡**。

## 8. 已定案（使用者拍板）
1. **自由位擴充成 4 個**：數字鍵 `1~4` 各施展一個自由位。**這是最大的一塊**——見 §8a。
2. **舊鍵**：退役 `1/2` 切裝與空白鍵；保留 右鍵=習得、滾輪=換裝、`X`=換裝別名。
3. **配裝互動**：**拖曳為主 + 雙擊快速裝備**；退役 `restSel` 點選式（避免兩套並存）。

### 8a. 擴充自由位 2 → 4（資料模型改動，非純 UI）
現在騎士配裝只存**單一** `itemId`（卷軸）+ `abilityId`（習得）兩個欄位；戰鬥單位 `slots` 是
`[武器組0, 武器組1, 自由(item), 自由(ability)]`＝4 格、且自由格**型別寫死**。擴充成 4 個自由位＝：
- **roster 配裝資料**：把 `itemId/abilityId` 收斂成**自由位陣列** `free: [{kind:'item'|'ability', id, count?}, …]`（長度 4），
  或保留舊欄位相容 + 加 `freeExtra`。存檔/跨關攜帶/`makePlayer` 重建都要跟著改。
- **`slots` 結構**：`[武器組0, 武器組1, 自由0, 自由1, 自由2, 自由3]`；自由格**型別跟隨內容**（放卷軸＝item/次數、放習得＝ability/CD），
  `SLOT_COUNT` 由 4 → 6。
- **出身 kit**：現在每個出身給 1 卷軸 + 1 習得＝填 2 個自由位，其餘 2 格開局空著（靠 loot/休息填）。
- **HUD**：自由位列從 2 格畫成 4 格，鍵位提示 `1~4`。
- **休息房配裝**：右側面板的「自由位」區從 2 格變 4 格，拖曳落點對應到 free 陣列的索引。
- **戰鬥鍵**：`1~4` → `useFreeSlot(kn, 2+n)`（第 n 個自由位）。
> 風險：這是本次最深的改動（動到 roster 配裝資料模型 + 存檔）。**建議排成 Phase 0 先做、先讓舊 2 格擴成 4 格能用**，再進 UI 重排。平衡（4 個自由位是否過強）待玩測。

## 9. 分階段實作
0. **擴充自由位 2 → 4（Phase 0，見 §8a）**：roster 配裝資料模型 + `slots`/`SLOT_COUNT` + 出身 kit + HUD 4 格 + 戰鬥鍵 `1~4`。先讓「4 個自由位」在現有 UI 下能用、能存檔、能跨關。
1. **右側面板骨架**：`drawKnightPanel`（裝備上半 + 倉庫下半），先用點選（既有 equip 函式）驗證資料流與型別守門。移除扈從配裝卡、加只讀隊伍列。
2. **拖曳輸入**：休息房接 mousedown/move/up + ghost + 落點型別判定 → 呼叫 equip/unequip；雙擊快速裝備；退役 `restSel`。
3. **戰鬥操作收尾**：滾輪換裝（維持）、`X` 別名；退役 1/2-切裝與空白；HUD 鍵位提示定案。
4. **打磨**：offers/TARGET 指定對象改讀新隊伍列；版面微調、動效。

## 10. 風險 / 注意
- 拖曳輸入要和既有 `restClick`（點選）不打架：實作階段 1 先點選、階段 2 疊拖曳時，把點選收斂或明確共存。
- 型別守門要涵蓋副手規則（雙手武器不能帶副手、副手只能盾或單手近接——`makePlayer` 的 `mkSet` 已有規則，UI 要一致）。
- 隊伍列若沒了會斷掉 offers 的「指定對象」——**務必保留**。
- 數字鍵改動會影響肌肉記憶，HUD 要清楚標新鍵位。
